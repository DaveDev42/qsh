name: fuzz-smoke

# Build-and-smoke gate for `fuzz/` (M8 DoD: "parser 타깃당 누적 ≥72
# fuzz-hours 무crash", `docs/ROADMAP.md`). This is NOT that 72-hour
# accumulation — a background/local-only job, see `fuzz/README.md` — it is
# a fixed, deterministic `-runs=<N>` pass over every target so a target
# that fails to build, or that crashes on its own seed corpus, is caught
# on every push instead of only whenever someone remembers to run it by
# hand.
#
# One job, one build: it loops `cargo fuzz run <t> -- -runs=<N>` over
# `cargo fuzz list`'s own output rather than a hard-coded target list, so
# a newly added `fuzz_targets/*.rs` is smoke-tested automatically — no
# separate matrix leg to remember to add, and no way for a target to
# silently escape CI by omission from a list here.
#
# Kept as its own workflow file, entirely separate from `ci.yml`'s
# `ci-ok` required-check aggregation, for two reasons:
# - `fuzz/` needs a nightly toolchain + `cargo-fuzz` install that the six
#   stable-toolchain gates (`CLAUDE.md`) must never depend on or be slowed
#   by — a separate job in a separate file can't accidentally end up on
#   the same dependency chain as `fmt`/`clippy`/`test`/`arch`/`deny`.
# - A failing target fails the job. The run step carried
#   `continue-on-error: true` for the harness's first two weeks; every run
#   from 2026-09-02 to 2026-09-14 came back green, so the guard came off.
#   The job is still outside `ci-ok`'s `needs`, so a red check here does
#   not block a merge by itself.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: fuzz-smoke-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  smoke:
    name: fuzz-smoke
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      # Deliberately does NOT honor the root rust-toolchain.toml (stable
      # 1.98.1, CLAUDE.md's "toolchain constraint") -- cargo-fuzz needs
      # nightly's -Z sanitizer flags, which is the whole reason fuzz/ is
      # kept out of the workspace in the first place (fuzz/README.md).
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: nightly
          cache-directories: fuzz/target
      # `fuzz/Cargo.lock` is its own lock; the workspace-root `deny` job
      # (`ci.yml`) never points at it, so advisories in this graph went
      # unscanned by CI until this step. Advisories only: licenses and
      # bans stay off because this crate is not a shipped artifact
      # (`docs/ROADMAP.md` M10 decision record, Q7).
      #
      # Prebuilt binary rather than EmbarkStudios/cargo-deny-action, for
      # the reason `ci.yml`'s `deny` job records: that action's docker
      # image floats independently of its tag and broke against
      # `rust-toolchain.toml`'s pin.
      #
      # Runs from the repo root, so it picks up the root `deny.toml`
      # (`[advisories]`: the rustsec db url and `yanked = "deny"`); the
      # `[licenses]`/`[bans]`/`[sources]` sections of that file are not
      # consulted by `check advisories`. Placed before the cargo-fuzz
      # install because that step builds cargo-fuzz from source.
      #
      # `--locked` makes this step verify the committed
      # `fuzz/Cargo.lock` itself rather than a graph `cargo metadata`
      # re-resolves on the fly; it also turns the "lock goes stale after
      # a workspace version bump" risk (`fuzz/README.md`'s Cargo.lock
      # scope section) into a visible failure here instead of a silent
      # in-memory fixup.
      #
      # A green run here does not stay green on its own: the RUSTSEC
      # database can publish a new advisory against an unchanged lock, so
      # a run that was green yesterday can go red today with no diff to
      # blame. The remedy is a lock bump in a commit of its own, not a
      # workflow change.
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-deny
      - name: Advisories for the fuzz lock
        run: cargo deny --locked --manifest-path fuzz/Cargo.toml check advisories
      - name: Install cargo-fuzz
        run: cargo install cargo-fuzz --locked
      # A fixed run count, not a time limit, per the task's own "CI must
      # be deterministic" rule -- this is a build-and-crash-check, not a
      # coverage exercise, so 4096 runs against a seeded corpus is enough
      # to catch "doesn't build" and "panics on its own seeds" without
      # making this job's runtime a function of runner load.
      #
      # The loop keeps going after a failing target so one bad target does
      # not hide the others; the step exits non-zero at the end and names
      # every target that failed.
      - name: Smoke-run every fuzz target
        id: smoke
        working-directory: fuzz
        run: |
          set -u
          failed=()
          for t in $(cargo fuzz list); do
            echo "::group::cargo fuzz run $t -- -runs=4096"
            if ! cargo fuzz run "$t" -- -runs=4096; then
              failed+=("$t")
            fi
            echo "::endgroup::"
          done
          if [ "${#failed[@]}" -gt 0 ]; then
            echo "Failed targets: ${failed[*]}"
            exit 1
          fi
