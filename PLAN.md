# PLAN.md — M9: 사람용 표면

M8의 코드 스텝이 끝나고 남은 DoD 넷을 §0.2로 이월한 채, 이 문서는 M9 실행 계획으로 전면 교체됐다(M8 자체의 마감 판정은 그 넷이 닫힐 때 `docs/ROADMAP.md`에 적는다). 구속 근거: `docs/ROADMAP.md` M9 절(범위 (a)~(j) — `:118`, 명시적 out — `:119`, DoD — `:120-126`, 크기 내역 S1~S10 — `:127`, 결정 기록 Q1~Q5 — `:128-133`), 마일스톤 마감 공통 절차(`docs/ROADMAP.md:25-30`), `docs/PRD.md` §6·§11·§15(SC1), `docs/CLI.md` §2.3·§2.4·§2.5·§3.2·§6.11·§6.12·§6.13·§6.17·§10, `docs/adr/0012-human-surface-naming.md`·`0013-cert-file-exchange.md`·`0014-address-default-port.md`·`0017-acl-toml-not-written.md`(0015·0016은 예약됨), `docs/design/architecture.md` §1·§7, `docs/design/testing.md` L6. 이 계획과 ROADMAP.md의 편집은 main 세션 전용이다.

M9는 CLI/JSON 축만 건드린다. M8이 얼린 wire 계약(`docs/design/protocol.md` §16)에 M9는 프레임을 더하지 않는다 — 이름 결정권도 `TrustInviteReq`/`TrustAcceptReq`라는 CLI 요청 타입에 붙고 wire의 자칭 값은 오늘의 `device_id` 그대로다(ADR-0012 결정 6, `0012-human-surface-naming.md:30`). 예외가 하나 있다. 2026-09-19에 범위로 들어온 SOCKS `-D`(Step 12)가 capability `dial-filter.v1`로 보호되는 `StreamHeader` 필드 하나를 더한다. 프레임은 늘지 않는다(ADR-0019 결정 3·4).

## 0. 착수 조건

### 0.1 Step 1 이전에 받아야 하는 사용자 결정 둘

- [x] ADR-0014 상태 확정. `docs/adr/README.md:3`이 상태 어휘를 `승인됨`(확정) / `제안됨`(결정 절은 다 냈으나 사용자 확정 전 초안) / `예약됨` 셋으로 정의하고, ADR-0014는 색인(`docs/adr/README.md:20`)과 파일 헤더(`0014-address-default-port.md:4`) 양쪽에서 `제안됨`이다. 그 ADR의 결정 1~5·7·9가 Step 1과 Step 5에서 코드로 착륙하므로, 확정 전에 구현을 열면 사용자 확정 전 초안을 근거로 구현하고 그 구현을 근거로 상태를 올리는 순환이 된다. 확정이 나기 전에는 Step 1을 열지 않는다. §8 열린 질문 1. **2026-09-17 확정** — 사용자의 전면 자율 진행 지시에 따라 세션이 `승인됨`으로 올렸다(결정 절 무변경, `docs/adr/0014-address-default-port.md:4`·`docs/adr/README.md:20`). Step 1 개봉. 사용자가 뒤집으면 Step 1의 변경을 되돌린다.
- [x] `serve --to` 모드의 unit 라벨 `<mode>` 토큰. Step 4의 doctor 경로 상수와 Step 8의 install 대상이 같은 상수를 공유하므로 값이 늦게 오면 Step 8이 막힌다. Step 4는 이 값에 묶이지 않게 설계한다(토큰을 상수 하나로 격리하고 값 확정은 Step 8로 미룬다 — §4.1 #8). §8 열린 질문 2. **2026-09-17 결정** — 토큰은 `reverse`를 유지한다. `docs/deploy/service.md:48`의 예시와 이미 배포된 fleet unit이 `io.qsh.reverse`이고, Step 8의 `qsh service status`가 그 기존 unit을 찾아야 하며, ADR-0012 결정 4가 v1 내내 구 표기를 유지하므로 라벨만 바꿀 이유가 없다(§8 열린 질문 2와 같은 결정).

### 0.2 M7·M8에서 열린 채 넘어온 DoD 넷

넷 중 어느 것도 M9의 코드 스텝을 막지 않는다. 다만 M9 DoD 1은 M7 DoD 1에 직접 종속이고(비교 대상이 그 측정치다), M8 DoD 4는 §6.0(M8판)이 운영자에게 넘긴 판단을 그대로 승계한다.

- [ ] M7 DoD 1 — 스톱워치 baseline 3회: 한 번도 설정한 적 없는 두 장비가 README만 보고 `qsh user@host`까지 5분 이내, 독립 3회. 미실행이고 사람이 해야 한다. 기준은 `docs/campaigns/m7-stopwatch.md`에 사전 고정됐고 회차 환경은 `scripts/stopwatch/round.sh`가 준비하지만 재는 대상이 사람 시간이다(같은 문서 §9). `docs/ROADMAP.md:132`가 Q4로 순서를 못박았다 — 현행 표면으로 먼저 재지 않으면 M9 DoD 1의 "단축 기록"이 성립하지 않는다. 실행 자리는 Step 0. 소유: 사람.
- [x] M8 DoD 2 — 24h/100-session soak: idle listener ≤30MB, 세션당 buffer ≤8MB, 사이클 중 fd 무증가. **2026-09-21 마감.** run #6(트리 8c4f319, 2026-09-19 17:21 UTC 시작, 86564.262s 완주)이 `run.sh: PASS`로 끝났다 — idle listener 27308 KiB, 세션당 1215.0 KiB, listener fd 11→12(quarters 델타 -5), dial_exhausted 0, dead_sessions 0. 기록은 `docs/campaigns/m8-soak.md` §5~§8 "run #6 결과". run #5의 verdict FAIL은 `ROADMAP.md:112`의 세 축이 아니라 `soak.rs`가 별도로 거는 DIAL_EXHAUSTED 8건 때문이었고 원인은 호스트 nftables 규칙이었다(§8 "run #5 결과"). run #6은 그래서 §2 9번대로 네트워크 네임스페이스 안에서 돌렸다. §7 태그 정책의 재지정 규칙대로 `v0.1.0-alpha.3` 대상은 run #6이 실제로 돈 트리 `8c4f319`다(§7 재지정 항목). 태그 문면과 생성은 사람이 한다. 소유: soak 회차.
- [ ] M8 DoD 3 — 실기기 mobility ≥60회: Wi-Fi↔테더링 자동 유지+resume ≥95%, migrated/resumed 분해 보고. 통과 기준은 `docs/campaigns/m2-mobility.md`에 사전 정의. 사람이 실행한다. 소유: 사람.
- [ ] M8 DoD 4 — wire freeze 후 독립 리뷰 계약(SC7): 문면은 `docs/design/protocol.md` §16에 "초안 — 발효 전"으로 서 있고 발효와 리뷰 계약은 M8 §6.0 판정 대기다. 선택지는 둘뿐이었다 — (가) 지금 예약하고 freeze를 예약일 6주 전으로 앞당긴다, (나) 예약 없이 freeze만 진행하고 리뷰를 미루며 PRD를 개정한다(`docs/ROADMAP.md:169` 리스크 5). 저장소 밖 조직 액션이라 에이전트가 대신할 수 없다. 소유: 운영자(M8 §6.0 승계). M9는 이 판정을 기다리지 않지만, §16 발효 소커밋에 딸린 잔여 셋(CLOSE `0x1003` 이중 이름 처분, README Security posture 한 줄, DoD 4 문구)은 M9 기간에도 M8 소관으로 남는다.

## 1. DoD 체크리스트 (ROADMAP M9)

- [ ] DoD 1 — SC1 스톱워치 재측정: 새 표면으로 독립 3회, 5분 이내, `docs/campaigns/m7-stopwatch.md` 선례 형식을 계승한 캠페인 문서로 baseline(M7 DoD 1 측정치, Q4에 따라 현행 표면으로 먼저 잰다) 대비 단축 기록. 근거: Step 9 ⑧(캠페인 문서 사전 정의) + Step 11 이전 사람 측정. §0.2 M7 DoD 1에 종속. 소유: 사람(측정 3회 + Step 0의 baseline 3회). 에이전트 몫은 캠페인 문서 사전 정의뿐이다.
- [ ] DoD 2 — doctor 신규 7종: 각각 안정된 code로 진단되는 것을 실행 가능한 메시지와 함께 테스트로 고정하고, `EXPECTED_DOCTOR_CODES`와 CLI.md §6.17 표를 같은 커밋에서 갱신. 근거: Step 4(6종) + Step 5(`[serve].to`↔구 `[reverse].controller` 충돌 1종 — 그 config 키가 서는 커밋).
- [ ] DoD 3 — 문구 표본 8종 축자 테스트: forward loopback 처방, `-D` 거절 문구(capability 부재 — 2026-09-19에 옛 "현행 유지 문구"를 Step 12가 대체하고, 2026-09-20에 ADR-0020이 역방향 거절을 걷었다), 재상환 실패의 host 전용 진단, 초대 출력의 후보 주소 열거, 포트 충돌 `--bind` 처방, 페어링 직후 pin 이름·매칭 규칙 부재 고지, `auth_path` 누락 host 진단, `assuming port 4433` 한 줄. 근거: Step 3(7종) + Step 4(`acl_ca_auth_path_missing` 1종 — 문면 상수가 `DiagnosticId` variant를 요구하므로 Step 3 커밋에 담기면 동결 set 게이트가 붉어진다, `crates/qsh-core/src/doctor.rs:116-122`). 페어링 고지 1종은 Step 3이 세우고 Step 7이 같은 상수를 개정하며 축자 테스트도 같은 커밋에서 갱신한다(ADR-0017 결정 3, `0017-acl-toml-not-written.md:32`).
- [ ] DoD 4 — 숨김 alias·config 이중 읽기 왕복 테스트: 구 표기 호출이 신 표기와 동일 op에 도달하고 신구 config 키가 동시에 있을 때의 우선순위가 고정된다. 근거: Step 5(`qsh reverse`·`[reverse].controller`), Step 7(`trust invite`/`trust accept`).
- [ ] DoD 5 — 신규 op 등록 완전성: 신규 op마다 새 JSON fixture + `REQUIRED_FIXTURES`(`crates/qsh-cli/tests/fixtures.rs:77-121`, 실측 43개) 등록, 기존 fixture 한 바이트도 안 고침, schemars 타입·`cli_v1_data_schema` arm·`CLI_V1_SCHEMA_COMMANDS` 등록·렌더러 2종·CLI.md 문서 행·man 항목이 모두 존재함을 등록 완전성 테스트로 확인(M5 `acl_registry` 선례 형식). 근거: Step 6(테스트 신설) → Step 7·8이 소비.
- [ ] DoD 6 — 마감 공통 절차 1·2: 구속 문서 태그 대조, README 동기화(서사·구조 전면 재작성은 명시적 out). 근거: Step 11.
- [ ] DoD 7 — SOCKS `-D`(2026-09-19 추가): 실제 `curl --socks5-hostname`이 qsh SOCKS listener를 통과하는 acceptance 테스트가 CI에서 건너뛰지 않고 초록이고, host-local 필터 거부와 ACL 거부가 각각 테스트로 고정되며, 옛 거절 계약의 fixture는 한 바이트도 바뀌지 않는다. 근거: Step 12.

## 2. 실행 단계 (PR 단위)

스텝 번호는 `docs/ROADMAP.md:127`의 크기 내역 S1~S10과 1:1이다. Step 0(사람 측정)과 Step 11(마감)은 그 내역에 없다.

계약 문서가 코드와 같은 커밋에 실린다는 규율. 신규 op의 CLI.md 행·man 항목·fixture·스키마 등록은 그 op이 서는 스텝에서 같은 커밋에 들어간다 — DoD 5의 등록 완전성 테스트가 그 스텝의 게이트이기 때문에 미룰 수가 없다. doctor code도 `EXPECTED_DOCTOR_CODES`와 CLI.md §6.17 표가 같은 커밋이라고 `docs/ROADMAP.md:122`가 직접 요구한다. README도 마찬가지다 — ADR이 지정한 README 델타는 그 표기를 바꾸는 스텝이 같은 커밋에서 갚는다(§3 README 항 참고). 그래서 Step 9(S9)에 남는 것은 어느 한 스텝도 소유하지 않는 횡단 문서면뿐이다.

### Step 0 — SC1 baseline 3회 (사람, 크기 내역 밖)

Q4(`docs/ROADMAP.md:132`)가 현행 표면 선측정을 못박았고 DoD 1이 그 측정치를 비교 대상으로 삼는다. Step 1~8이 main에 얹히면 "현행 표면" 빌드는 트리에서 사라지므로, 그 뒤의 baseline은 태그된 트리를 되짚어 빌드해야만 가능해진다.

- M7 DoD 1 baseline 3회를 `525a2a5` 트리(=§7 태그 대상)에서 측정한다. 기준은 `docs/campaigns/m7-stopwatch.md`에 이미 사전 고정돼 있다.
- 측정에 쓴 바이너리의 sha256을 같은 문서의 회차 표에 기록한다 — `docs/campaigns/m8-soak.md:27`이 "24h 내내 같은 바이너리(같은 sha256)"를 회차 요건으로 고정한 것과 같은 규율이다.
- Step 0이 미완인 상태로 Step 1을 main에 얹어도 된다. 다만 그 시점부터 baseline은 태그 트리 재빌드로만 가능하다는 사실을 Step 1 (a) 첫 줄에 남긴다.

### Step 1 — 주소 기본값: 포트 생략 시 4433 (S1, 0.3ew)

근거 ADR: ADR-0014 결정 1(`0014-address-default-port.md:18`), 결정 2(`:20-24`), 결정 3(`:26-30`), 결정 4(`:32-39`), 결정 5(`:41`). 선행: §0.1의 ADR-0014 확정(안 나오면 이 스텝을 열지 않는다). Step 3·5·6이 이 결과에 기댄다. Step 0이 미완이면 baseline은 이후 태그 트리 재빌드로만 가능하다.

**(a) 범위:** ① 정규화 함수 하나를 세운다 — 포트가 없으면 `:4433`을 붙이고 빈 문자열은 항등(ADR-0014 결정 2). 기본 포트는 4433 하나로 단일화하고 "peer 기본 포트"를 새로 만들지 않는다(결정 1); 상수는 `crates/qsh-core/src/serve.rs:21`의 `pub const DEFAULT_BIND: &str = "[::]:4433"`이 쓰는 값과 같은 자리에서 나오게 한다. 배치는 §4.1 #1 — 결정 2(`:22`)가 "계약층인 `qsh-proto`를 먼저 검토한다"고 요구했고 대괄호 IPv6를 이미 정확히 다루는 `format_host_port`/`parse_forward_spec`이 `crates/qsh-proto/src/wire.rs`에 있으므로, 그 검토의 결론과 사유를 이 스텝에 기록한다. 대괄호 없는 순수 IPv6 리터럴(`::1` 류)의 포트 유무 판정은 결정 2(`:24`)가 명시적으로 S1에 위임했다(§4.1 #11). ② 쓰기 시점 적용 — `Ops::trust_add`(`crates/qsh-core/src/ops/mod.rs:506`)와 `Ops::trust_accept`(`:640`)가 저장 전에 정규화한다(결정 3). op 등록 자리(`TrustAddOp`/`TrustAcceptOp`의 `const COMMAND`, `:209-213`·`:223-227`)와 구분한다. ③ 읽기 시점 적용 — 지점은 `host::resolve_forward`(`crates/qsh-core/src/ops/host.rs:231`)가 반환하는 `ForwardEntry.address`와 `Ops::trust_list`(`crates/qsh-core/src/ops/mod.rs:563`)가 돌려주는 `TrustPeer.address` 파생값 두 곳이다(ADR-0014 결정 4). `resolve_host`(`crates/qsh-core/src/trust/mod.rs:197-199`)는 대상이 아니다 — 그 함수는 store 내부 구조체의 차용을 돌려주므로 여기서 정규화하려면 시그니처나 저장 구조를 바꿔야 하고, 자기 doc(`trust/mod.rs:190-196`)도 "실제 해석이 필요한 호출자는 `resolve_forward`로 가라"고 적어 두었다. `resolve_forward` 한 곳을 고치면 `Ops::host_list`, `Ops::resolve_host_route`/`Ops::host_get`, `resolve_peer_address`(`ops/mod.rs:892`, `qsh exec`와 `reverse::target::dial_and_register`의 controller dial 공유), `Ops::doctor`의 `same_as_controller` 중복 판정(`crates/qsh-core/src/ops/doctor.rs:411`)이 동시에 덮인다. 라이브 역방향 등록 주소(`resolve_route`의 `HostRoute::Reverse` 분기)는 사람이 쓴 파일이 아니라 데몬이 관측한 값이므로 정규화 대상이 아니다(결정 4 `:34`). `trust.toml`은 다시 쓰지 않는다(결정 4 `:39`, `docs/ROADMAP.md:118` (d)의 "파일은 불변"). ④ `resolve_forward`의 `source` 판정(`ops/host.rs:252-258`, 지금은 `hosts == trust` 생짜 문자열 비교)을 정규화 후 비교로 옮긴다 — `hosts.toml=mac:4433` / `trust.toml=mac`을 `"hosts"` 불일치로 보고하면 거짓 신호다(결정 4 `:37`). ⑤ `assuming port 4433` 한 줄을 stderr에 낸다 — 실제로 포트를 채운 경우만, stdout엔 절대 쓰지 않는다(결정 5). 발화 지점은 셋이고 그중 둘이 이 스텝이다(`Ops::trust_add`, `Ops::trust_accept`). 세 번째는 Step 5의 `serve --to` 인자 해석이다. 읽기 경로(`trust.list`/`host.list`/`host.get`)는 이 줄을 내지 않는다(결정 5 `:41`). 문면 상수는 `qsh-core`가 갖고 CLI는 그대로 출력한다(`docs/design/architecture.md` §1); 축자 테스트는 Step 3이 8종 묶음으로 가져간다. ⑥ 이월 항목 — `hint_alias`(`crates/qsh-core/src/ops/host.rs:441`)가 벗긴 별칭의 조회 키 trim을 주소 정규화와 같은 자리에서 처리한다(§6 행 i). ⑦ additive 보장 — 신규 op 0, 신규 JSON 필드 0, 신규 error code 0, 신규 doctor code 0.

**(a)-추기 — Step 1 착지(`d5eef05`).** §4.1 #1·#11·#12의 결론을 근거와 함께 남긴다. #1 정규화 함수의 배치: `qsh-core`의 `trust` 모듈에 둔다 (`crates/qsh-core/src/trust/mod.rs`의 `normalize_peer_address`). ADR-0014 결정 2(`:22`)가 요구한 `qsh-proto` 우선 검토는 실제로 했고 결론은 기각이다. 기각 사유는 기본 포트의 출처다 — 결정 1이 "peer 기본 포트"의 신설을 금지하므로 값은 `serve.rs`의 `DEFAULT_BIND`에서 나와야 하고 arch 매트릭스상 `qsh-proto`는 아무 크레이트에도 의존하지 않으므로 `qsh-proto`에 둔 함수는 그 값을 읽을 수 없다. 대신 `qsh-proto`가 이미 정확히 다루는 부분은 그대로 호출한다: 대괄호 IPv6 표기는 `qsh_proto::wire::format_host_port`가 만들고 이 함수가 괄호 규칙을 다시 유도하지 않는다. 고지 문면도 같은 자리에 둔다. operator 대상 문장의 정본은 `qsh-core`이고 frontend는 건네받은 것만 찍는다는 architecture.md §1 규율을 따랐다(선례: `ops::INVITE_CODE_PROMPT`). 기본 포트 상수는 `serve::DEFAULT_PORT`로 `DEFAULT_BIND`에서 const 유도되고 고지 문면이 그 값과 어긋나면 컴파일 타임 assert가 빌드를 깬다.

#11 대괄호 없는 순수 IPv6 리터럴: 호스트로 읽는다. `::1`을 그대로 넘기면 "가장 오른쪽 `:` 뒤가 숫자면 포트" 규칙이 호스트 `:`·포트 `1`로 오판하므로 `Ipv6Addr` 파싱이 성공하는 입력은 포트 판정 이전에 걸러 `format_host_port`로 괄호를 씌운 뒤 4433을 붙인다. 판정 순서가 규칙 자체다.

#12 포트 범위: 강제하지 않는다. `host:0`·`host:99999`는 바이트 그대로 통과하고 dial 시점에 지금과 똑같이 실패한다. `1..=65535` 규율은 `-L`/`-R` 스펙 문법(`docs/CLI.md:482`, `qsh_proto::wire::parse_forward_spec`)에만 남고 peer 주소로 넓히지 않는다. 넓히려면 §6.11에 additive 문장이 필요한데 ADR-0014는 정규화만 정했고 범위 검증을 새로 계약하지 않았다.

이 스텝의 PLAN.md 인용 줄번호는 실제 트리와 어긋난다. `ops/mod.rs` 쪽 인용(`:506` `trust_add`, `:640` `trust_accept`, `:563` `trust_list`, `:892` `resolve_peer_address`, `:932` `server_name_for`)은 Step 2 착지 이후의 파일에서 대략 144줄 아래로 밀렸다. 이후 스텝은 줄번호가 아니라 심볼 이름으로 찾는다.

**(b) 건드릴 파일:** `crates/qsh-core/src/ops/host.rs`(`resolve_forward` + `source` 비교 + `hint_alias` 조회 키), `crates/qsh-core/src/ops/mod.rs`(`trust_add`/`trust_accept`/`trust_list`), `crates/qsh-core/src/trust/mod.rs` 또는 `crates/qsh-proto/src/wire.rs`(정규화 함수 — §4.1 #1의 배치 결론에 따른다), `crates/qsh-core/src/serve.rs`(상수 정합), `crates/qsh-cli/src/cli.rs:549-564`(`TrustAddArgs.--address` 도움말 문면), `docs/CLI.md:585`(`--address <host:port>`가 포트 명시를 전제한 예제)·§6.1 host 해석 우선순위·§6.11.

**(c) 테스트:** 유닛 표 — 포트 있음/없음, IPv6 bracket 표기, 대괄호 없는 순수 IPv6 리터럴(§4.1 #11의 판정대로), 빈 문자열. 쓰기 왕복 — `trust add <name> --address host`가 `trust.toml`에 `host:4433`을 남긴다. 읽기 불변 — 포트 없는 주소를 직접 써 둔 `trust.toml`을 `trust list`·`resolve_forward`가 4433으로 해석하되 파일 바이트 해시가 전후 동일하다. `source` 상호작용 — `crates/qsh-cli/tests/hosts_toml.rs`의 병합·우선순위 회귀를 유지한 채 "`hosts.toml=mac:4433` / `trust.toml=mac` → `source=both`" 한 줄을 더한다. negative — `HostRoute::Reverse`로 뜨는 라이브 등록 주소는 정규화되지 않는다. stderr negative 2건 — `trust list`/`host list`는 포트 없는 주소를 해석해도 `assuming port 4433`을 내지 않는다. 기존 fixture 게이트 — `every_required_fixture_exists`(`crates/qsh-cli/tests/fixtures.rs:919`)와 전 fixture 대조 green, 신규 fixture 0. ADR-0014 결과(`0014-address-default-port.md:65`)가 열거한 8개 fixture(`trust.list.json`, `trust.add.json`, `trust.accept.json`, `host.list.json`, `host.get.json`, `host.list.with_hosts_toml.json`, `host.get.with_hosts_toml.json`, `error.TRUST_REQUIRED.json`)는 `address`를 `"<address>"`로 마스킹하고 있어(마스킹 자리는 `crates/qsh-testkit/src/fixtures.rs:118`) 정규화가 기존 바이트를 바꾸지 않는다 — 이 게이트는 그 사실의 회귀 확인이다.

**(d) 완료 판정:** 위 테스트 전부 green, `trust.toml` 바이트 불변 단언, `source` 비교가 정규화 후로 옮겨진 것이 `hosts_toml.rs`로 고정, CLI.md의 포트 전제 문장이 같은 커밋에서 갱신, 신규 op·error code·doctor code 0, §4.1 #1·#11·#12의 결론이 근거와 함께 기록됨.

### Step 2 — `pair accept`의 `code` 선택 인자화 + 프롬프트 (S2, 0.2ew)

근거 ADR: ADR-0013 결정 8(`0013-cert-file-exchange.md:29`). 선행: 없음(Step 1과 독립). 후속: Step 7이 이 표면을 `pair accept`로 개명한다.

**(a) 범위:** ① `TrustCmd::Accept`의 `code` 위치 인자(`crates/qsh-cli/src/cli.rs:478-483`)를 `Option<String>`으로 바꾸고 `--code-stdin`을 신설한다. TTY 표준입력이면 에코 없는 프롬프트, 파이프(비-TTY)면 `--code-stdin`으로 표준입력을 끝까지 읽어 앞뒤 공백을 제거한 값을 코드로 쓴다 — `qsh_proto::pairing::parse_invite_code`가 Crockford 심볼 외 문자를 거부하므로 후행 개행을 그대로 넘기면 첫 시도부터 실패한다(ADR-0013 결정 8). `--json`/`--jsonl`에서는 프롬프트를 열지 않고, code 인자도 `--code-stdin`도 없이 machine mode로 부르면 `INVALID_ARGUMENT`다. ② 도움말 두 줄 — "fingerprint는 공개값이라 히스토리에 남아도 위험이 아니다", "소비된/만료된 코드는 `INVITE_TTL`(`crates/qsh-core/src/trust/pairing.rs:64`, 10분) 1회용이라 재사용 힘이 없다"를 같은 결정이 요구한다. ③ 개명은 하지 않는다 — 이 스텝은 현 표기 `trust accept`에 붙고, Step 7이 `pair accept`로 옮기며 구 표기를 숨김 서브커맨드로 남긴다. 그래야 Step 5가 세우는 숨김 서브커맨드 기계 하나로 두 개명을 같이 덮는다. ④ 정책의 소재 — "machine mode에서는 프롬프트 없이 거부"라는 판정은 `qsh-core`가 갖는다(`docs/ROADMAP.md:118` (j)). 구현은 순수 resolver 하나로 둔다: (code 유무, `--code-stdin` 여부, machine mode 여부, stdin이 TTY인지)를 받아 "이 문면으로 프롬프트를 열어라 / 이 code를 쓰라 / `INVALID_ARGUMENT`"를 돌려주고, CLI는 지시된 I/O만 수행한다. `TrustAcceptReq.code`(`crates/qsh-proto/src/types.rs:742-751`)의 타입은 `String` 그대로 — 계약 무변경(§4.1 #2). ⑤ TTY 감지·에코 끄기의 선례는 `trust add`의 대화형 확인 프롬프트(`crates/qsh-cli/src/main.rs:1122-1158`, `is_terminal()` 사용).

**(b) 건드릴 파일:** `crates/qsh-cli/src/cli.rs`(`TrustCmd::Accept` 인자 + 도움말 두 줄), `crates/qsh-cli/src/main.rs`(프롬프트·stdin 배선), `crates/qsh-core/src/trust/pairing.rs` 또는 `ops/mod.rs`(resolver), `docs/CLI.md:692-693`(`qsh trust accept <address> <code>` syntax 행)·§6.11의 invite 소비 규칙 절.

**(c) 테스트:** resolver 표 테스트 4조합(code 제공 / `--code-stdin` / TTY 프롬프트 지시 / 둘 다 없고 machine mode → `INVALID_ARGUMENT`). trim 회귀 2건 — `printf 'xxxx-…-89ab\n'`(후행 개행)과 `printf '  xxxx-…-89ab  '`(앞뒤 공백) 두 입력이 모두 성공한다. CLI e2e 둘 — `--code-stdin --json` 성공, code 없이 `--json` 호출이 `INVALID_ARGUMENT` envelope + exit 255. `crates/qsh-cli/tests/jsonl_purity.rs` 회귀 — 프롬프트 문면이 stdout에 섞이지 않는다. `crates/qsh-cli/tests/exit_code_matrix.rs`에 code 부재 machine mode row 1줄. fixture는 신설하지 않는다 — 신규 op가 아니므로 DoD 5의 "신규 op마다" 대상이 아니고 `error.INVALID_ARGUMENT.json`이 이미 `REQUIRED_FIXTURES`에 있다(§4.1 #3).

**(d) 완료 판정:** 위 테스트 green, machine mode에서 프롬프트 호출 경로가 타입/분기 어느 쪽으로도 도달 불가함을 mutation으로 실증(프롬프트 분기를 machine mode에 열면 e2e가 FAIL), trim을 지우면 개행 테스트가 FAIL, CLI.md §6.11 syntax 행과 도움말 두 줄이 같은 커밋, 신규 스키마 arm·fixture 0.

**(a)-추기 — Step 2 착지 (2026-09-17, main 세션).** 커밋 d71cb38. 리졸버는 `crates/qsh-core/src/ops/mod.rs`의 순수 함수 `resolve_invite_code_source(code, code_stdin, machine_mode, stdin_is_tty, echo_can_be_suppressed)`로 뒀다 — (b)가 열어 둔 두 자리 중 `ops/mod.rs`를 고른 이유는 `ops/tunnel.rs`의 `parse_local_forwards`가 같은 모양(CLI 사전 판정을 qsh-core에 두는 순수 함수)으로 이미 서 있어서다. 결과는 `UseCode` / `ReadStdin{suppress_echo}` / `Prompt{text}` 셋이고 거부는 `OpError`다(code 없음과 tty 위 `--code-stdin`은 machine mode에서 `INVALID_ARGUMENT`, echo를 못 끄는 플랫폼(cfg(not(unix)))의 tty 프롬프트는 `UNSUPPORTED`). (c)의 "4조합"은 결정표 6행이 됐다: 사람 모드인데 stdin이 tty가 아니고 code도 없는 행(`exit_code_matrix`가 양 모드 exit 일치를 요구하므로 이 행도 `INVALID_ARGUMENT`)과 tty 위에서 `--code-stdin`을 준 행(echo를 끄고 읽는다)이 추가다. 메시지 문면까지 결정표 테스트로 고정했다. 테스트는 (c) 그대로에 `tui_expect`의 실제 pty 테스트(프롬프트 중 ECHO off와 복귀, tty 위 `--json`은 대기 없이 거부)를 더했다 — expectrl(ptyprocess)이 exec 전에 ECHO를 끄고 자식을 띄우므로 `sh -c 'stty echo && exec'`로 켠 뒤 qsh를 띄운다. (d)의 mutation 둘은 리뷰 단계에서 실증했다(machine mode에 프롬프트 개방 → 3 FAIL, trim 제거 → 2 FAIL). 신규 fixture·스키마 arm 0. 게이트: fmt·clippy(`-D warnings`, ubuntu+windows)·deny·arch green, `x86_64-pc-windows-gnu` 타깃 cargo check·rustdoc green. nextest는 로컬(MBP16)의 syspolicyd 실행 검증 정체로 목록 단계에서 끝나지 않아 CI로 판정했다 — run 35153668775(같은 커밋, 브랜치): ubuntu·ubuntu-arm 1561 passed / 3 skipped, macos-14 1551 / 3, windows 1275 / 2. main 푸시 CI 35154177779·fuzz-smoke 35154177746 green. 첫 CI 회차가 잡은 셋(e2e가 machine mode 문면 대신 비-tty 문면을 기대, pty 테스트의 echo 전제, `--code-stdin` 도움말의 em dash를 콜론으로 바꾼 뒤 man 페이지 미재생성)과 Windows rustdoc의 `EchoSuppressed` intra-doc 링크(cfg(unix) 전용 항목)는 같은 커밋 안에서 고쳤다.

### Step 3 — 실패 문면 7종의 관측·영향·다음 명령 규율 (S3, 0.5ew)

근거 ADR: 분리 원칙은 ADR-0017 결정 4 — "원격 오류와 콘솔 진단을 분리한다. 실패 문면이 관측·영향·다음 명령을 갖는다는 일반 규칙에서 이 두 축은 예외로 남긴다"(`0017-acl-toml-not-written.md:38`). 개별 근거: `assuming port 4433` = ADR-0014 결정 5(`0014-address-default-port.md:41`), 포트 충돌 `--bind` 처방 = ADR-0014 결정 9(`:57`), 페어링 직후 pin 이름·매칭 규칙 부재 고지 = ADR-0017 결정 3(`0017-acl-toml-not-written.md:30-32`). `auth_path` 누락 host 진단(`acl_ca_auth_path_missing`)은 이 스텝이 아니라 Step 4 소유다 — `Diagnostic`이 `id: DiagnosticId`를 필수 필드로 갖기 때문에(`crates/qsh-core/src/doctor.rs:116-122`) 문면 상수를 세우는 순간 variant가 필요하고, variant가 생기면 동결 set 게이트 넷이 같은 커밋에서 `EXPECTED_DOCTOR_CODES`·CLI.md §6.17 표·산문 계수 두 곳을 함께 요구한다. 나머지 셋(forward loopback 처방, `-D` 현행 유지 문구, 재상환 실패의 host 전용 진단)과 "초대 출력의 후보 주소 열거"는 어느 ADR에도 축자 근거가 없다 — UNVERIFIED이고 문면 자체를 이 스텝이 확정한다(8종 주제 목록의 정본은 `docs/ROADMAP.md:123`).

선행: Step 1(`assuming port 4433` 상수가 Step 1에서 생기고 이 스텝이 축자 고정).

**(a) 범위:** ① 인벤토리 먼저 — 8종 각각의 현재 소유 상수를 file:line으로 확정하고 부재를 부재로 기록한다. `acl_ca_auth_path_missing` 행은 "문면 소유가 Step 4"로 표시한다. 확인된 계층 둘: `qsh-core`의 `Diagnostic{id, code, message, remedy}` 상수 패턴(`crates/qsh-core/src/doctor.rs:116-122`가 그 struct, `CONTROLLER_UNREACHABLE`·`PEER_UNTRUSTED`(`:181-185`)가 그 형태이고 `main.rs:973-975`·`:1038-1040`이 그 `.message`를 그대로 찍는다)과 `OpError{code, message, retryable, details}`(`docs/design/architecture.md` §2). ② 7종을 관측·영향·다음 명령 3부로 다시 쓴다. 항목별 소유 지점:

| 문구 | 소유 지점 | 근거 |
|---|---|---|
| `assuming port 4433` | Step 1이 세운 상수 1개, 발화 3지점(`Ops::trust_add` `ops/mod.rs:506`, `Ops::trust_accept` `:640`, Step 5의 `serve --to` 해석) | ADR-0014 결정 5 `:41` |
| 포트 충돌 `--bind` 처방 | `crates/qsh-core/src/serve.rs:83`(`run_serve`의 `"cannot listen on {bind}: {err}"`)과 `crates/qsh-core/src/reverse/listen.rs:262`(같은 문면의 별도 bind 경로) 두 곳 — 한쪽만 고치면 `qsh listen` 쪽 충돌은 처방 없는 옛 문면으로 남는다 | ADR-0014 결정 9 `:57` |
| 페어링 직후 pin 이름·매칭 규칙 부재 고지 | `crates/qsh-core/src/server/mod.rs:2778`(`Server::serve_pairing_connection`이 `crate::pairing::respond`에 넘기는 pin 콜백). `trust/pairing.rs`의 `InviteRecordFile`은 초대 레코드 파일 쪽이고 고지를 내는 프로세스가 아니다 | ADR-0017 결정 3 `:30` |
| forward loopback 처방 | `qsh_core::tunnel::REMOTE_FORWARD_LOOPBACK_ONLY_MESSAGE`(`docs/CLI.md:482` 절이 이미 이 상수를 정본으로 지목) | UNVERIFIED(ADR 없음) |
| `-D` 현행 유지 문구 | `crates/qsh-core/src/ops/tunnel.rs:108`의 `"SOCKS dynamic forwarding (-D) is a P1 feature"`. clap 정의는 `crates/qsh-cli/src/cli.rs:114-129`(`Cli` 레벨)과 `:386-393`(`TunnelCmd::Open`) 두 자리이고 문면 소유가 아니라 파싱 자리다 | UNVERIFIED(ADR 없음) |
| 재상환 실패의 host 전용 진단 | 인벤토리에서 확정(현재 상수 부재 가능성) | UNVERIFIED(ADR 없음) |
| 초대 출력의 후보 주소 열거 | 신설. `TrustCmd::Invite` 자기 doc이 "No address to reach *this* device is known here — the operator supplies one out of band"(`crates/qsh-cli/src/cli.rs:470-473`)라고 적어 둔 상태이므로 문면 재작성이 아니라 없는 정보를 만드는 작업이다 | UNVERIFIED(ADR 없음) |

③ "초대 출력의 후보 주소 열거"는 셋을 별도로 확정한다 — ⓐ 후보 주소의 출처(바인드된 소켓 / 로컬 인터페이스 열거, `crates/qsh-core/src/doctor/probe.rs` 재사용 여부 실측, §4.1 #13), ⓑ human/stderr 전용이고 `trust.invite` envelope에 필드 0 추가(ADR-0012 결정 9 `:38`이 `trust.invite`/`trust.accept`의 `data` 필드 이름·타입 불변을 못박았다), ⓒ `crates/qsh-cli/tests/jsonl_purity.rs` 회귀와 `trust.invite.json` byte 무변경. ④ 페어링 고지는 2단계 문면이다. 이 스텝 판은 "이 이름은 상대가 자칭한 값이다"를 함께 적는다 — ADR-0017 결정 3(`:32`)이 `--as` 착지 전 구간에 그 단서를 요구하기 때문이다. Step 7이 `--as`를 착륙시키며 같은 상수를 개정하고 축자 테스트도 그 커밋에서 갱신한다. ⑤ 예외 축 둘을 문서와 테스트 양쪽에 사유와 함께 남긴다 — 원격 오류(`PairingError::as_wire_error` 매핑과 `PERMISSION_DENIED_MESSAGE`(`crates/qsh-core/src/acl/mod.rs:270`)의 균일 문면)와 콘솔 진단(tracing stderr 구조화 진단, `docs/CLI.md` §6.13)은 3부 강제 대상이 아니다(ADR-0017 결정 4). 의도적 제외를 사유 명시 목록으로 두는 관례는 `crates/qsh-core/tests/schema_commands_registry.rs:126-133`의 `EXCLUDED`와 같은 모양이다. ⑥ ACL 문면을 건드리는 항목은 균일성 게이트와 같이 움직인다 — `PERMISSION_DENIED_MESSAGE`의 균일성 규율(`acl/mod.rs:270`, 그 exhaustive 테스트는 `crates/qsh-testkit/tests/acl_uniformity.rs`이고 `crates/qsh-core/src/acl/registry.rs:1-11`의 모듈 doc이 그것을 지목한다, `crates/qsh-core/src/broker/mod.rs:154`)과 CLI.md §3.2·architecture.md §6 슬라이스 축자 대조(`crates/qsh-core/tests/acl_docs.rs:146`·`:160`)가 동시 게이트다. ⑦ 포트 공유 사실 한 줄을 README `Known limitations`(`README.md:528` 절)에 더한다 — ADR-0014 결정 9(`:57`)가 요구하고 오늘 그 절에는 그 사실이 없다.

**(b) 건드릴 파일:** `crates/qsh-core/src/doctor.rs`(문면 상수), `crates/qsh-core/src/serve.rs`·`crates/qsh-core/src/reverse/listen.rs`(포트 충돌 두 지점), `crates/qsh-core/src/server/mod.rs`(페어링 고지), `crates/qsh-core/src/ops/tunnel.rs`(forward loopback·`-D`), `crates/qsh-core/src/trust/pairing.rs`(재상환 실패 — 인벤토리 결과에 따른다), `crates/qsh-core/src/ops/mod.rs`(초대 출력 후보 주소), `crates/qsh-core/tests/doctor_docs.rs`·`crates/qsh-core/tests/acl_docs.rs`(축자 테스트 확장), `docs/CLI.md`(인용 문장), `README.md`(포트 공유 한 줄). README는 축자 강제 대상이 아니라는 일반화는 쓰지 않는다 — `doctor_docs.rs:38-47`의 `readme_quotes_the_controller_unreachable_diagnostic_verbatim`이 `CONTROLLER_UNREACHABLE`의 `message`/`remedy`를 README에서 verbatim으로 요구하고, `doctor_docs.rs:82-88`의 제외 주석은 §6.17 계열 진단에만 걸린다.

**(c) 테스트:** 7종 각각 축자 테스트 1건 — `doctor_docs.rs`(문면 verbatim + 구간 슬라이스)와 `acl_docs.rs`(§ 헤딩부터 다음 `#`까지 슬라이스, `acl_docs.rs:105`의 `heading_section_slice`) 두 틀을 그대로 쓴다. 포트 충돌은 두 경로 각각 verbatim 1건 — 기존 `qsh_listen_bind_conflict_is_one_stderr_line_exit_255_and_zero_stdout`(`crates/qsh-cli/tests/exit_code_matrix.rs:706`)을 확장하고 serve 쪽 쌍둥이 1건을 더한다. 3부 구조 테스트 — 7종의 message/remedy가 관측·영향·다음 명령 세 요소를 비우지 않는다(구조 단언이고 문장 표절 방지가 아니다). 예외 2축이 3부 강제 목록에서 사유와 함께 제외됐음을 단언하는 테스트 1건. mutation — 문면 1글자 변경이 아니라 발화 지점별로 정의한다: `assuming port 4433`은 같은 상수가 3곳에서 나오므로 문면 변경 하나가 3곳을 동시에 잡아 버린다. 발화 라인 자체를 지우는 mutation을 지점별로 돌려 (7종 문면 + 포트 충돌 2지점 + `assuming port 4433` 2지점, Step 5 착륙 후 3지점) 대응 테스트가 각각 FAIL하는지 실증한다.

**(d) 완료 판정:** 7종 축자 테스트 green + 발화 지점별 mutation 전건 CAUGHT, CLI.md 인용 문장과 README 포트 공유 한 줄이 같은 커밋, 예외 2축이 문서·테스트 양쪽에 남는다, `acl_uniformity.rs`와 `acl_docs.rs` 기존 단언 무손상, 페어링 고지가 Step 7 개정 대상임이 테스트 주석에 명시됨.

**(a)-추기 — Step 3 착지 (2026-09-18, main 세션). 스텝을 3a/3b로 갈랐다.** 커밋 `858acb5`가 3a이고 T1~T6 여섯 문면과 예외 목록 둘, README `Known limitations` 한 줄을 담는다. 3b는 (a)③의 "초대 출력의 후보 주소 열거" 하나이고 아직 열지 않았다 — §4.1 #13은 3b가 답한다. 가른 기준은 새 OS 표면이다. 나머지 여섯은 문면과 상수 작업이고 근거가 ADR-0014 결정 5·7·9, ADR-0017 결정 3·4에 있거나 이 스텝에 위임된 것뿐인데, 후보 주소 열거만 지금까지 네트워크를 전혀 건드리지 않던 `qsh trust invite`에 커널 라우트 질의를 새로 들인다(`connect`만 하고 `send`를 하지 않으므로 와이어로 나가는 바이트는 0이다). 그 표면을 ADR로 기록할지, 후보 0건 구멍을 `getifaddrs` 폴백으로 메울지는 §8에 사용자 결정 사안으로 올렸다. 3a만 먼저 착지시키면 여섯 문면이 M9 안에서 굳고 3b가 결정을 기다려도 스텝 전체가 멈추지 않는다.

**문면 소유와 상수.** 둘은 기존 상수를 값만 교체했다(`ADDRESS_PORT_ASSUMED_NOTICE`, `REMOTE_FORWARD_LOOPBACK_ONLY_MESSAGE`). 하나는 두 자리에 흩어져 있던 format 문자열을 상수 하나로 걷었다(`BIND_UNAVAILABLE_REMEDY` + `bind_unavailable`). 셋은 신설이다(페어링 pin 고지 상수 셋 + `pairing_pin_notice`, `PAIRING_INVITE_REPLAY_NOTICE`, `DYNAMIC_FORWARD_UNSUPPORTED_GUIDANCE`). (a)②의 표가 "두 곳"으로 적은 포트 충돌을 상수 하나와 조립 함수 하나로 합친 근거는 둘이다. 저장소에 같은 모양의 선례가 서 있고(`DYNAMIC_FORWARD_UNSUPPORTED_MESSAGE` + `dynamic_forward_unsupported()`), 결정 9가 경고한 "한쪽만 고치면 `qsh listen` 쪽은 처방 없는 옛 문면으로 남는다"를 이 구조가 불가능하게 만든다. 문면을 공유해도 발화 라인은 둘이므로 지점별 mutation 실증은 그대로 성립한다.

**`trailing_port` 단언을 `names_only_port`로 갈았다.** 3부 문면은 기본 포트를 문장 중간에 두므로 문자열 끝에서 역방향으로 숫자를 읽는 `trailing_port`로는 컴파일되지 않는다. `names_only_port`는 위치를 고정하지 않고 문면 안의 독립 숫자 런이 전부 기본 포트인지 전수로 본다 — "그것만 부른다"까지 보는 것이 옛 단언의 doc이 지키던 성질이고, 존재 검사로 약화시키면 두 번째 리터럴이 두 번째 진실 소스가 된다. 선행 0(`04433`)과 u16 초과 런은 거부한다. 후자는 const 문맥에서 u32 overflow가 되면 깔끔한 `false`가 아니라 `E0080` 평가 오류가 되기 때문이다. `trailing_port` 자체는 남아 있다 — `DEFAULT_PORT`가 `DEFAULT_BIND`에서 포트를 뽑는 자리는 여전히 문자열 끝이다.

**`acl_row_present`의 출처.** 설계 1차 판이 이 boolean의 데이터 소스를 비워 뒀고, 실측으로 두 제약이 나왔다. `Arc<dyn Authorizer>`로는 답할 수 없다 — `Authorizer` trait의 메서드는 `check` 하나뿐이고 principal 열거 API가 없다. 대표 action 하나로 `check`를 찔러 대신하는 방법도 틀린다(그 principal을 부르는 행이 있는데 다른 action만 grant 한 경우 deny가 나와 "행이 없다"로 오분류된다). 페어링 시점에 `acl.toml`을 새로 읽는 방법은 더 나쁘다 — 정책은 기동 시 1회만 로드되므로, 기동 뒤 추가된 행은 파일에는 있지만 강제되지는 않고 그 상태에서 "그 행의 grant를 그대로 물려받는다"가 거짓이 된다(결정 2 `:28`이 doctor remedy에 재시작 문장을 필수로 넣은 것과 같은 시점 위험). 그래서 `load_or_deny`가 `Arc::new`로 타입을 지우기 직전에 들고 있는 그 `Policy`에서 `PinnedPrincipalIndex`를 함께 뽑는 `load_or_deny_with_index`를 형제로 뒀다. 기존 호출자(`ops/doctor.rs`)는 시그니처가 그대로다. 매칭은 `auth_path`가 pin인(생략 시 기본값 포함) 행만 세고 `Missing`/`Invalid`는 빈 집합이다 — 그 상태에서 강제되는 것은 `DenyAll`이라 아무 행도 걸리지 않는 것이 옳다. 부수 효과로 PRESENT 분기에는 재시작 주의가 필요 없어졌고, ABSENT 분기만 남았다(그쪽의 다음 행동이 파일 편집이고 그 편집이 재시작 없이는 반영되지 않는다).

**고지의 출력 경로.** pin 콜백과 재상환 분기는 둘 다 `qsh-core` 안이고 CLI 콜스택이 닿지 않는다. `run_serve`에 `on_notice` 클로저를 하나 더 받아 CLI가 `stderr_note!("qsh serve: {notice}")`로 찍는다 — `policy_diagnostic` 클로저가 바로 위에 선 같은 모양이고, `qsh-core`가 직접 stderr에 쓰지 않는다는 architecture.md §1 규율을 지킨다.

**T6은 채널을 갈랐다.** `error.UNSUPPORTED.json`이 현행 `message`를 전체 envelope `assert_eq!`로 박고 있고 fixture는 append-only라 envelope 문면을 늘릴 자리가 없다. `details` 우회도 현재 `null`이라 같은 바이트 변경이다. 관측은 envelope에 동결로 남기고 영향·다음 명령은 `DYNAMIC_FORWARD_UNSUPPORTED_GUIDANCE`로 내서 clap long_help 두 자리와 README, `docs/CLI.md` §6.9가 소비한다. 이 분할 자체는 이 스텝에 위임된 권한으로 섰다. 반면 `docs/man/qsh.1`의 `--json` 우선순위 표기 충돌 수정은 ADR-0019 결정 3에서만 나오는 요구이고 그 ADR은 아직 `제안됨`이라 3a에서 뺐다 — §0.1이 ADR-0014에 걸었던 것과 같은 게이트다. 착지 중 리뷰가 ADR을 읽지 않은 상태에서 같은 충돌을 독립적으로 다시 찾았다.

**예외 목록을 표 둘로 나눴다.** ADR-0017 결정 4가 면제한 둘(원격 페어링 실패 문면과 `PERMISSION_DENIED_MESSAGE`의 균일 문면, tracing 구조화 진단)은 `EXCLUDED`에, 저장소 규율이 막는 하나(`-D` envelope)는 `CHANNEL_CONSTRAINED`에 뒀다. 사유가 다르기 때문이다 — 앞은 처방을 얹지 않는 것이고 뒤는 처방을 다른 채널로 내는 것이다. 역방향 단언도 걸었다: `PERMISSION_DENIED_MESSAGE`와 `AlreadyConsumed`가 3부 표에 들어오면 `acl_uniformity.rs`가 깨지므로 그것을 이 파일에서 막는다.

**착지 중 고친 것 둘.** 하나, bind 처방이 모든 `SetupError`에 붙고 있었다. `SetupError`에는 `Tls`와 `Quic` variant도 있어서 TLS 설정 실패에 "`--bind`를 다른 포트로"를 붙이면 고칠 수 없는 다음 명령을 주는 셈이다. `bind_setup_error`를 두고 `SetupError::Bind`만 처방을 받게 했다. 둘, pin 고지의 다음 명령에 들어가는 이름은 상대가 자칭한 값이고 `validate_device_name`은 제어문자·bidi·zero-width·길이만 보므로 공백·따옴표·세미콜론이 통과한다. 복사해 붙이라고 내주는 줄이므로 principal을 POSIX 홑따옴표로 감싸고 `'`는 `'\''`로 escape한다 — 감싸기만 하면 저장소가 유효한 이름으로 이미 단언한 `Dave's MacBook Pro`에서 따옴표가 먼저 닫힌다.

**보고만 하고 고치지 않은 것 하나.** `docs/CLI.md:47`이 `--quiet`를 "성공 시 진단 출력 억제"로 적었지만 트리의 `stderr_note!` 자리 어느 것도 `--quiet`를 보지 않는다 — `listening on`, `identity … fingerprint`, `shutting down`이 다 그렇고 `--quiet`가 거는 것은 tracing 계층뿐이다. 새 고지는 기존 자리 전부와 같은 자세이므로 이 스텝이 만든 문제가 아니고, 어느 쪽으로 맞추든 이 스텝이 건드리지 않는 명령들의 출력을 한꺼번에 바꾸는 결정이다. 별도 스텝이나 §8 항목으로 다룬다.

**(a)-추기 — Step 3b 착지 (2026-09-18, main 세션).** 커밋 `51455dc`가 3a가 미뤄 둔 "초대 출력의 후보 주소 열거" 하나를 담는다. §4.1 #13의 답은 `doctor/probe.rs` 재사용이 아니다 — 그쪽 탐지기는 자기 모듈 doc이 `doctor.run`으로 범위를 못박고 있어서 `trust.invite`가 부르면 진단 모듈이 페어링 경로의 의존이 된다. 대신 같은 모양의 형제를 `trust/invite_address`에 세웠다. 순수한 부모(문면·`InviteAddressAdvice`·`assemble`·`port_from_bind_spec`)와 OS에 닿는 자식(`route`)으로 가른 것이 `doctor::probe`가 쓰는 탐지/분류 분리와 같은 구조다.

**새 OS 표면은 ADR이 아니라 threat-model 항목으로 기록했다.** ADR-0019가 `제안됨`에 머문 상태에서 3a가 `docs/man/qsh.1` 수정을 뺐던 것과 같은 게이트를 여기에도 걸면 스텝이 결정을 기다리며 멈춘다. 이 표면이 새로 여는 것은 진입점 하나뿐이고 — `qsh trust invite`가 커널 라우팅 테이블에 질의한다 — 그것은 `docs/design/threat-model.md` §3 진입점 표가 담는 종류의 사실이다. 프로토콜도 와이어 포맷도 JSON 계약도 바뀌지 않으므로 새 결정으로 기록할 것이 없다. 후보 0건 구멍을 `getifaddrs` 폴백으로 메우는 쪽은 별도 스텝으로 미뤘다 — 후보의 출처를 라우트 질의에서 인터페이스 열거로 바꾸는 일이고 열거가 내는 주소 집합의 필터 규칙을 새로 정해야 한다.

**와이어로 나가는 바이트는 0이다.** `UdpSocket::bind`로 unspecified에 묶고 `connect`로 목적지를 지정한 뒤 `local_addr`만 읽는다. `send`도 `recv`도 부르지 않는다 — connect된 UDP 소켓은 패킷을 내지 않고도 커널이 고른 소스 주소를 답한다. 목적지는 문서화 대역의 `SocketAddr` 상수 둘이고(`192.0.2.1:9`, `[2001:db8::1]:9`) `&str`이 아니다. 문자열이면 파싱이 아니라 resolver를 타므로 DNS 질의가 나갈 수 있다. `xtask arch`가 `invite_address` 파일과 그 디렉터리 양쪽에서 `.send`/`.recv`를 금지해 이 성질을 기계로 건다.

**기계 가드 둘을 세웠다.** 문면과 테스트만으로는 두 변이가 잡히지 않는 것을 실증했기 때문이다. 하나, `ops.invite_address_advice()`를 human 클로저 밖으로 끌어올리면 게이트 일곱이 전부 green인 채 machine 모드가 매 호출마다 라우트 질의를 한다 — 버려지는 관측값은 stdout에 한 바이트도 쓰지 않으므로 jsonl 순수성 회귀도 `finish`의 게으름 테스트도 눈이 없다. `route`에 `AtomicUsize` 호출 카운터를 두고, `run`에서 갈라낸 `dispatch`를 `--json`과 human 두 번 태워 카운터를 읽는다. 이 테스트가 통합 테스트일 수 없는 이유는 카운터가 자식 프로세스 주소 공간에 살기 때문이다. 둘, `local_addr()`를 `peer_addr()`로 바꾸면 operator에게 `192.0.2.1:4433`을 건네면서도 일곱 게이트가 green이다. 관측된 IP가 질의 자신의 목적지와 같지 않다는 단언을 `qsh-core` 쪽에, stdout이 두 문서화 주소를 담지 않는다는 단언을 `init_trust.rs` 쪽에 각각 걸었다. 라우트가 없는 기계에서 공허하게 참이 되므로 조건부 단언이 아니다.

**문면 넷을 고쳤다.** 착지 전 리뷰가 상수 둘의 절 여섯 가운데 넷이 거짓인 경우를 실기계에서 재현했다. ① 후보 0건 문면의 "a missing route"는 두 경로 중 하나에서만 참이다 — 커널이 소스 주소를 답하지 않은 경우와, 답했으나 zone index 때문에 `host:port`에 쓸 수 없는 IPv6 link-local이 걸러진 경우가 있고 뒤쪽은 라우트가 있었다. ② 다음 명령으로 `qsh doctor`를 지목한 것은 무동작이다 — `doctor_connectivity_findings`는 named target 안에서만 probe하고 첫 `trust invite`를 돌리는 호스트에는 그 pin이 없다. 저장소가 `no_route` remedy에서 이미 쓰는 `ip route`/`route -n` 표기로 바꿨다. ③ 머리글이 주소 *집합*을 주장했지만 질의가 내는 것은 IP family당 최대 하나, 기본 경로의 소스 주소뿐이다 — 두 번째 NIC도 VPN/overlay도 빠진다. 리뷰가 tailnet 주소가 조용히 누락되는 것을 재현했다. ④ 포트 출처 주장이 "미설정이면 4433"만 말했지만 fallback 조건은 셋이다 — 미설정, config 읽기·파싱 실패, bind spec의 포트가 `u16` 파싱 실패. TOML을 깨는 줄이 든 config로 재현했고 그 상태에서 `qsh serve`는 `CONFIG_ERROR`로 뜨지도 않는다.

**상수 길이 상한을 271자로 잡았다.** 설계 1차 판이 제안한 418자·703자는 저장소의 다른 문면 상수와 자릿수가 다르다 — 기존 상수는 45자에서 271자 사이에 있고 최댓값이 `PAIRING_INVITE_REPLAY_NOTICE`다. 넷을 고치면서 늘어난 분량은 배경 설명을 rustdoc과 `docs/CLI.md` §6.11로 옮겨 상쇄했다(265자·263자).

**고치지 않고 공개한 것 둘.** 하나, `[serve].bind`의 host 절반을 버리므로 loopback에 바인드된 서버도 LAN 후보를 광고한다. 고치려면 후보의 출처가 라우트 질의에서 설정된 bind host로 바뀌는지를 결정해야 하고 그건 이 스텝 범위 밖이다. 기본값이 unspecified라 이 경우는 operator가 명시적으로 loopback에 묶은 때뿐이고, 머리글이 도달성을 이미 부정하므로 찍히는 값이 거짓말은 아니고 쓸모없을 뿐이다. 둘, 찍히는 IPv6 후보가 RFC 8981 privacy extension 임시 주소면 하루 안에 돌아가는데 `trust accept`는 그것을 durable하게 pin한다. 둘 다 `docs/CLI.md` §6.11에 한 문장씩 적었다.

**착지 중 지운 것 하나.** 끌어올리기 변이를 소스 텍스트 매칭으로 잡으려던 테스트가 동어반복이었다 — `include_str!("main.rs")`가 찾는 바늘이 그 단언 자신의 리터럴에 있어서 dispatch arm이 무엇이든 통과한다. 카운터 가드가 같은 변이를 실제로 잡으므로 지웠고, 그 테스트를 가리키던 주석 둘은 카운터 가드 이름으로 고쳤다.

### Step 4 — doctor 진단 6종 + 동결 set 확장 (S4, 0.5ew)

근거 ADR: `acl_principal_unmatched`·`acl_ca_auth_path_missing` 2종은 ADR-0017 결정 2가 code 이름과 매칭 규칙까지 정한다(`0017-acl-toml-not-written.md:20-26`). 나머지 4종(서비스 미등록, systemd linger 미설정, macOS LaunchAgent 로그인 세션 한계, `bindv6only`)은 어느 ADR에도 결정문이 없다 — ADR-0017 결과 절이 "이 ADR이 정하지 않는다. 같은 설계의 doctor 스텝과 `qsh service` 스텝 소관이다"라고 명시적으로 넘겼다(`:46`). 즉 이 스텝이 그 결정 지점이다.

선행: Step 3(문면 규율 틀). 후속: Step 5가 7번째 code를, Step 8이 unit 경로 상수를 소비한다.

**(a) 범위:** ① `DiagnosticId`(`crates/qsh-core/src/doctor.rs:37-52`, 현재 14 variant)에 6 variant를 더하고 `DiagnosticId::code()`의 match(`:64-81`)와 `EXPECTED_DOCTOR_CODES`(`:90-105`, 현재 14개 정렬 배열)를 같이 확장한다. 같은 커밋에 CLI.md §6.17 표(`docs/CLI.md:1053-1068`) 6행과 산문 계수 두 곳(`docs/CLI.md:750` "진단 코드 14종", `:1051` "**14종 진단 코드**")을 갱신한다. `crates/qsh-core/tests/doctor_docs.rs:139-140`의 주석은 옛 줄번호(`:752`·`:1030`)를 인용하고 있으니 실제 산문 자리와 구분해 적는다. `:1051` 괄호의 "(재사용 5종·신설 9종 — …)" 분해는 `cli_md_prose_doctor_code_count_matches_expected_len`이 보는 두 패턴("진단 코드 N종"/"N종 진단 코드")에 걸리지 않아 자동 게이트가 없다 — 같은 커밋에서 손으로 갱신하고 (d)의 체크 항목에 둔다. ② 6종: 서비스 미등록, systemd linger 미설정, macOS LaunchAgent 로그인 세션 한계, `bindv6only`, `acl_principal_unmatched`, `acl_ca_auth_path_missing`. status 어휘는 기존 4값(`ok`/`warn`/`error`/`info`)만 쓴다 — 새 status 값 0(`docs/CLI.md:1014-1040`). ADR-0017 결과 절(`:46`)이 서비스 미등록을 info, systemd linger·LaunchAgent 한계를 warn으로 예고했고 그 예고를 따른다(§4.1 #9). ③ `acl_ca_auth_path_missing`의 문면 상수는 이 스텝 소유다(Step 3의 인벤토리가 그렇게 표시한다). DoD 3의 축자 테스트도 이 커밋에서 Step 3의 틀로 함께 세운다. ④ 두 acl 진단의 remedy에는 "행을 추가한 뒤 `serve`/`listen`을 재시작해야 반영된다"를 반드시 넣는다 — ADR-0017 결정 2(`:28`)가 그렇게 요구하고, 근거는 `qsh acl check`가 호출마다 파일을 다시 읽어 재시작 전에도 allow로 보이는 비대칭이다. ⑤ `acl/load.rs`의 `minimal_policy_example`(`crates/qsh-core/src/acl/load.rs:373`)을 역할별 allow 목록을 받도록 넓힌다 — 그 함수의 `EXAMPLE_ALLOW`(`:374`)는 `exec.run`/`session.open`/`session.list`/`session.attach`/`session.control` 다섯뿐이고 `host.reverse`가 없어서 `listen` 역할 예시를 만들 수 없다(ADR-0017 결정 2 `:24`). 예시 TOML을 새 표면이 다시 타이핑하면 anti-drift 규율을 깬다. ⑥ 모듈 doc 산문 — `crates/qsh-core/src/doctor.rs:31`의 "14 variants, one per `docs/CLI.md` §6.17 finding code"를 20으로(Step 5 후 21) 갱신한다. ADR-0017 결과(`:48`)가 같은 커밋 대상으로 열거하지만 자동 게이트가 없어 빠지면 아무도 잡지 않는다. ⑦ 판정 로직은 `crates/qsh-core/src/ops/doctor.rs`, 플랫폼 탐지는 `crates/qsh-core/src/doctor/probe.rs`(모듈 선언 `doctor.rs:24`)에 둔다 — `doctor.rs` 본체의 `#[cfg]` 금지 규율(모듈 doc `:14-23`)을 깨지 않는다. ⑧ unit 경로 상수와 모드 추론을 이 스텝이 정본으로 세운다: `~/Library/LaunchAgents/io.qsh.<mode>.plist`와 `~/.config/systemd/user/qsh-<mode>.service`(`docs/ROADMAP.md:118` (g), 템플릿은 `docs/deploy/service.md:15`·`:106`). `<mode>` 토큰의 값은 §8 열린 질문 2 대기이므로 이 스텝은 토큰을 단일 상수로 격리해 두고 값 확정은 Step 8로 미룬다 — 완료 판정도 "값이 맞다"가 아니라 "토큰이 단일 상수에 격리됐다"다. 모드 추론은 이 시점에 `[listen]`과 구 `[reverse].controller`(`crates/qsh-core/src/config.rs:905`, 자기 doc이 "Reserved, not currently read"라고 적어 둔 필드)만 읽는 순수 함수로 서고, Step 5가 `[serve].to`를 additive로 더한다. ⑨ exit code는 여전히 항상 0이다(`docs/CLI.md:1012`) — finding은 data다. ⑩ 환경 탐지는 주입 시임을 거쳐 조립 경로를 탄다. 실기계의 linger·plist 상태를 강제할 수 없으므로 조건부 단언(`if let Some(finding) = …`)을 금지한다 — M7 Step 6이 같은 함정에서 진단 배선을 지워도 green이던 사례를 두 건 기록했다.

**(b) 건드릴 파일:** `crates/qsh-core/src/doctor.rs`, `crates/qsh-core/src/doctor/probe.rs`, `crates/qsh-core/src/ops/doctor.rs`, `crates/qsh-core/src/acl/load.rs`(`minimal_policy_example`/`EXAMPLE_ALLOW`), `crates/qsh-core/src/config.rs`(모드 추론이 읽는 필드), `docs/CLI.md` §6.17 표·산문 계수 두 곳, `crates/qsh-core/tests/doctor_docs.rs`.

**(c) 테스트:** 동결 set 두 게이트 — `expected_doctor_codes_matches_every_diagnostic_id_variant_exactly`(`doctor.rs:353`)와 `expected_doctor_codes_is_sorted_and_deduplicated`(`:386`)가 20종으로 통과. 문서 게이트 — `cli_md_names_every_frozen_doctor_code`(`doctor_docs.rs:124`)와 `cli_md_prose_doctor_code_count_matches_expected_len`(`:150`). 6종 각각 유발 테스트 — 스텁 주입으로 발화 1건과 비발화 0건을 양쪽 단언. remedy 재시작 문면 축자 테스트 2건(Step 3의 틀). `minimal_policy_example`이 역할별로 다른 allow 목록을 내는 테스트(기존 `minimal_policy_example_fills_in_actual_pinned_peer_names`(`acl/load.rs:1573`) 무손상). mutation — 6종의 배선 라인을 각각 지웠을 때 대응 테스트가 FAIL하는지 6/6. `exit_code_matrix.rs`의 doctor row 무변경(항상 0). `cargo check --target x86_64-pc-windows-gnu`로 Windows leg 컴파일 확인(linger/launchd 탐지가 `cfg` 밖으로 새지 않는지).

**(d) 완료 판정:** `EXPECTED_DOCTOR_CODES` 20종 set-equality green, CLI.md 표 6행·산문 계수 두 곳·`:1051` 괄호 분해가 같은 커밋, `doctor.rs:31` 모듈 doc의 variant 수가 20으로 갱신, mutation 6/6 CAUGHT, `acl_ca_auth_path_missing`의 축자 테스트 green, remedy 재시작 문면 2건 green, unit 경로 상수의 `<mode>` 토큰이 단일 상수에 격리되고 Step 8이 그것을 소비할 수 있는 모양, exit 0 계약 무변경.

### Step 5 — `serve --to` 개명 + 숨김 서브커맨드 + config 이중 읽기 + PRD (S5, 0.5ew)

근거 ADR: ADR-0012 결정 2(개명과 순수 outbound 모드, `--bind` 동시 지정은 `INVALID_ARGUMENT`, `--offered-name` → `--name` 개명이 의미론을 바꾸지 않는다 — `0012-human-surface-naming.md:26`), 결정 4(구 표기 숨김 서브커맨드를 v1 내내 유지 — `:28`), 결정 5(`[serve].to` 신설 + 구 `[reverse].controller` 이중 읽기, 우선순위, 충돌 시 serve/service install은 `CONFIG_ERROR`·doctor는 exit 0 유지 + finding — `:29`), 결정 1(역할 어휘 표 — `:18-25`), 결과 절(`:42-45`). `--to` 인자의 해석 순서(이름으로 trust 조회 우선, 실패 시 정규화된 주소로 재조회, 이름이 항상 주소보다 우선)는 ADR-0014 결정 7(`0014-address-default-port.md:45-51`).

선행: Step 1(주소 정규화 — `--to host`의 포트 채움), Step 4(doctor 동결 set과 모드 추론 순수 함수), §8 열린 질문 6(hide된 서브커맨드의 man 처분 — 실측 후 ADR과 어긋나면 사용자 결정).

**(a) 범위:** ① `Command::Serve`(`crates/qsh-cli/src/cli.rs:280-285`)에 `--to <listener|host:port>`와 `--name <N>`을 더한다. `--to`가 주어지면 인바운드 bind를 하지 않는다. `--bind`와 동시 지정은 clap `conflicts_with`가 아니라 파싱만 하고 `qsh-core`가 `INVALID_ARGUMENT`로 거부한다 — clap 충돌은 usage error(exit 2)가 되어 ROADMAP이 요구한 코드가 나오지 않는다. 선례는 `-L`/`-R`/`-D`의 파싱-후-검증(`cli.rs:83-87`의 doc가 그 규율을 적는다). `--to` 인자 해석에서 포트를 채우면 `assuming port 4433`을 stderr에 낸다 — ADR-0014 결정 5(`:41`)와 결과(`:63`)가 지정한 세 번째 발화 지점이다. ② 하부는 기존 경로를 그대로 쓴다 — `run_reverse`(`crates/qsh-cli/src/main.rs:1002-1052`)가 부르는 `qsh_core::reverse::target::run_reverse_observed`. 신규 wire 0, 신규 op 0(`qsh serve`/`qsh listen`은 operation이 아니라 장기 실행 모드 — `docs/CLI.md:98`). ③ 구 `qsh reverse`(`cli.rs:315-327`)는 `#[command(hide = true)]`로 남아 같은 op에 도달한다. 인자 이름의 비대칭이 계약이다 — 구 `Reverse`는 positional `controller` + `--offered-name`을 그대로 유지하고 신 `Serve --to`는 `--name`을 쓴다(ADR-0012 결정 2, 결과 `:44`의 "파싱 결과는 오늘과 동일 구조"). `[serve].name`은 신설하지 않고 `[reverse].offered_name`(`crates/qsh-core/src/config.rs:910`)의 의미론(controller에 이 peer의 alias가 없고 `[listen].allow_advertised_names`가 켜져 있을 때만 적용)도 불변이다. `long_running_setup_mode`(`crates/qsh-cli/src/main.rs:910-917`)와 `command_name`(`:1190`)의 `Reverse` 브랜치를 지우지 않는다. ④ config 이중 읽기 — `ServeConfig`(`crates/qsh-core/src/config.rs:440`)에 `to: Option<String>`을 신설하고 구 `[reverse].controller`(`:905`)를 실제로 읽기 시작한다. 우선순위는 ADR-0012 결정 5(`:29`)가 정한 4단 그대로다: `--to`(CLI) > `[serve].to` > `[reverse].controller`(구 키) > 없음(순수 인바운드 `serve`). 두 config 키가 둘 다 있고 값이 다르면 `qsh serve`와 `qsh service install`은 `CONFIG_ERROR`로 fail closed하고(같은 값이면 통과), `qsh doctor`는 exit 0을 유지한 채 finding으로 report한다. CLI `--to`가 주어졌을 때 두 config 키의 값 충돌을 여전히 `CONFIG_ERROR`로 볼지는 ADR이 남긴 구멍이고 §4.1 #4b에서 확정한다. ⑤ 7번째 doctor code(값 충돌)를 이 커밋에서 추가한다 — `DiagnosticId` 1 variant + `EXPECTED_DOCTOR_CODES` 21종 + CLI.md §6.17 1행 + 산문 계수 두 곳 + `doctor.rs:31` 모듈 doc 계수. code 문자열은 ADR-0012가 확정하지 않았다(`:43`)므로 §4.1 #4에서 정한다. 동결 set을 두 커밋(Step 4의 6종, 여기 1종)에 나누는 것은 additive-only 규율에 저촉되지 않는다(`doctor.rs:90-96`의 "A code is added here only alongside a new `DiagnosticId` variant"). 값 충돌 진단은 `[serve].to`가 존재해야 도달 가능하므로 여기가 그 자리다. ⑥ 기존 `controller_unreachable` 표면 개정 — ADR-0012 결과(`:43`·`:44`)가 셋을 요구한다: `crates/qsh-core/src/ops/doctor.rs:379`의 `doctor_connectivity_findings`가 `[serve].to`를 먼저 보게 바꾸고(현재는 `:388`이 `config.reverse.controller`만 읽는다), `docs/CLI.md:1055`의 `controller_unreachable` 표 행과 `:1010`의 §6.17 도입 문장("인자 없는 `qsh doctor`는 `[reverse].controller`가 설정돼 있으면")의 조건문을 "`[serve].to` 또는 구 `[reverse].controller`가 설정돼 있으면"으로 갱신하고, `config.rs`의 `ReverseConfig::controller` doc 주석(`:892-904`의 "Reserved, not currently read")을 "serve/doctor/service install이 fallback으로 읽는다"로 고친다. 조건문만 바꾸고 문면 상수는 건드리지 않는다 — `doctor_docs.rs:51`·`:64`가 CLI.md와 PRD.md 양쪽에서 `CONTROLLER_UNREACHABLE`의 `message`/`remedy`를 축자 게이트한다. ⑦ 계약 문서면 — ADR-0012 결정 1·결과(`:18-25`, `:42`)가 요구하는 것 다섯을 이 스텝이 소유한다: ⓐ 역할 어휘 표(host/client × 인바운드/outbound)와 "controller는 listener이자 client 역할, target은 host 머신" 두 줄을 `README.md`와 CLI.md §6.13(`:871`)에 신설, ⓑ CLI.md §6.13의 `qsh reverse <controller>` 표기를 전부 `qsh serve --to <listener>`로 치환하고 구 표기 alias 행을 같은 절에 additive로 남기고 `--to`/`--bind` 동시 사용 거부를 명시, ⓒ CLI.md §6.12(`:757`)에 `--to` 행 추가, ⓓ CLI.md §10에 "숨김 서브커맨드 표기의 수명은 §6.13/§6.11이 정한다" 한 줄 신설, ⓔ `docs/design/architecture.md` §7 config 절에 `[serve].to`/`[reverse].controller` 서술. controller reachability 문안(`docs/CLI.md:882-884` = `docs/PRD.md:106-110`, 정본 공유)은 문면을 깨지 않는다. PRD는 §6(역방향 접속 예제 `qsh reverse controller.example.com` → `qsh serve --to controller`)과 §11 명령 요약(`docs/PRD.md:241-257`)의 표기를 옮긴다. ⑧ README 델타(§3 README 항) 중 이 스텝 몫 — 역할 표 신설과 역방향 접속 예제 전부(`README.md:34`, `:384`, `:391`, `:441`, `:533`, `:562`, `:564`, `:626`의 `qsh reverse` 표기). ⑨ man 재생성 — `cargo run -p xtask -- man` 뒤 `docs/man/qsh-serve.1`에 `--to`/`--name` 절이 실린다. ADR-0012 결과(`:42`)는 구 man 3면(`qsh-reverse.1`·`qsh-trust-invite.1`·`qsh-trust-accept.1`, 실측 모두 `docs/man/`에 있다)을 "숨김 서브커맨드 설명으로 유지"하기로 이미 정했다. 실측할 것은 `clap_mangen`이 `hide = true` 서브커맨드의 페이지를 생성하는지 하나뿐이다. 생성하면 ADR과 자동으로 맞고, 생성하지 않으면 `xtask/src/man.rs`의 파일 집합 동일성 게이트(`:93-124`의 `assert_eq!(checked_in, fresh)`, `run()`이 체크인된 `*.1` 전부를 먼저 지우고 다시 렌더한다)가 즉시 붉어지므로 두 분기 중 하나를 사용자 결정으로 받는다 — (가) `man.rs`에 "생성기 밖 유지 파일" 예외 목록을 사유와 함께 신설, (나) ADR-0012 결과 절의 man 문장을 Step 10에서 개정. §8 열린 질문 6. 실측은 이 스텝 착수 전에 하고 즉흥 결정하지 않는다.

**(b) 건드릴 파일:** `crates/qsh-cli/src/cli.rs`, `crates/qsh-cli/src/main.rs`, `crates/qsh-core/src/config.rs`, `crates/qsh-core/src/reverse/`(모드 분리 검증), `crates/qsh-core/src/doctor.rs`·`ops/doctor.rs`(7번째 code + `doctor_connectivity_findings`), `crates/qsh-core/tests/doctor_docs.rs`, `docs/CLI.md` §6.12·§6.13·§6.17·§10, `docs/PRD.md` §6·§11, `docs/design/architecture.md` §7, `README.md`, `docs/man/`, 필요 시 `xtask/src/man.rs`(열린 질문 6이 (가)로 나면).

**(c) 테스트:** 숨김 서브커맨드 왕복(DoD 4) — `qsh reverse <c>`와 `qsh serve --to <c>`가 같은 op·같은 인자 해석에 도달함을 clap 파싱 층과 dispatch 층 두 곳에서 단언하고, `qsh reverse c --offered-name n`과 `qsh serve --to c --name n`이 같은 `(controller, offered_name)` 조합으로 수렴함을 명시적으로 단언한다. `--help`에 구 이름이 안 보이는 것(ADR-0012 결과 `:43`)도 1건. 기존 `reverse_requires_controller_and_offered_name_is_optional`(`crates/qsh-cli/src/cli.rs` 테스트 모듈) 계열을 확장한다. config 우선순위 표 — (CLI `--to` 있음 / 없음) × (신 키만 / 구 키만 / 둘 다 같은 값 / 둘 다 다른 값) × (`serve`, `service install`, `doctor`): CLI `--to`가 없고 값이 다르면 앞의 둘은 `CONFIG_ERROR`, doctor는 exit 0 + finding 1건. CLI `--to`가 있으면 그것이 두 config 키 양쪽을 이기고, 그때의 충돌 처분은 §4.1 #4b의 확정대로 고정한다. `--to`와 `--bind` 동시 지정이 exit 255 + `INVALID_ARGUMENT` envelope(exit 2 아님)임을 `exit_code_matrix.rs` row로. 이름 vs 주소 우선순위(ADR-0014 결정 7) 테스트 + 정규화된 주소가 둘 이상의 pin과 일치하면 `INVALID_ARGUMENT`. `--to` 해석의 `assuming port 4433` 발화 1건(Step 3의 축자 틀). doctor 21종 set-equality·CLI.md 표·산문 계수·모듈 doc 계수 green. 기존 역방향 테스트 회귀 무손상 — `crates/qsh-cli/tests/reverse_e2e.rs`, `hosts_reverse.rs`, `reverse_blackout.rs`, `reverse_unreachable_diagnostic.rs`(그중 최소 1건은 구 표기로 남겨 숨김 서브커맨드 경로의 살아 있는 증거로 둔다). man byte-exact 게이트.

**(d) 완료 판정:** 위 전부 green, CLI.md §6.12·§6.13·§6.17·§10과 PRD §6·§11과 architecture.md §7과 README 역할 표·역방향 예제가 같은 커밋, man 게이트 green(열린 질문 6의 결론이 코드/문서에 반영됨), 신규 fixture 0·신규 op 0, `qsh listen`의 계약 무변경(`docs/CLI.md:887`·`:892`), `CONTROLLER_UNREACHABLE` 문면 상수 무변경 단언.

### Step 6 — 파일 교환 프로비저닝: `identity export` / `trust add --cert-file` / `trust add-ca` (S6, 0.7ew)

근거 ADR: ADR-0013 결정 1(ADR-0002 개정 — fingerprint 수동확인 + 인증서 파일 교환을 1급 fallback으로, `0013-cert-file-exchange.md:18`), 결정 3(`identity export [--out <path>]`, `--json`/`--jsonl`에서 PEM은 `IdentityExportData.cert_pem` 필드로만, `:20-21`), 결정 4(`trust add <name> --cert-file <pem|->`, `:22-23`), 결정 5(`trust add-ca <name> --cert-file <pem|->`, `:24-26`), 결과(`:36-41`).

선행: Step 1(주소 정규화 — `--cert-file`과 `--address`가 같은 저장 경로를 쓴다). 이 스텝이 DoD 5의 등록 완전성 테스트를 신설하고 Step 7·8이 그것을 소비한다.

**(a) 범위:** ① 신규 op 2 — `identity.export`, `trust.add_ca`(이름 §4.1 #6. ADR-0013 결과 `:36`이 `trust.add_ca`/`trust.addca` 중 확정을 CLI.md §2.4 개정 시점으로 넘겼다). `trust add --cert-file`은 기존 `trust.add` op의 additive 입력이다(신규 op 0): `TrustAddReq`(`crates/qsh-proto/src/types.rs:587-600`)에 `cert_pem: Option<String>`을 더하고 `TrustAddData`(`:601`)는 건드리지 않는다. ② `identity export` — `Operation` 패턴(`crates/qsh-core/src/ops/mod.rs:114`의 `const COMMAND`, `:183-240`의 impl 블록들)대로 `IdentityExportOp` + `Ops::identity_export`. `qsh identity`는 신설 명령 그룹이다 — `crates/qsh-cli/src/cli.rs:221-229`는 `Command::Init`이고 `Command::Identity`는 없다. `qsh init` 유지 여부와 `qsh identity init` alias 신설 여부는 ADR-0013 결정 3(`:20`)이 "이 ADR이 결정하지 않는다"로 남겼으므로 §4.1 #10에서 정한다(그 답이 `qsh-identity.1`의 내용과 man 면 수를 바꾼다). 소스는 `identity/device.pem`(`crates/qsh-core/src/identity/mod.rs:36`의 `CERT_FILE`)이고 `KeyStore`는 열지 않는다. 실패 모드 둘: identity가 아직 없으면 기존 `read_identity` 경로대로 `CONFIG_ERROR`("no local identity; run `qsh init` first", `crates/qsh-core/src/identity/mod.rs:275`), `--out`이 기존 파일과 겹치면 조용히 덮지 않고 거부한다. `--out`을 쓰면 두 모드 모두 결과에 파일 경로만 남고 PEM 본문은 stdout에 나오지 않는다. ③ `trust add --cert-file` — PEM 파싱·검증은 `qsh-core`가 하고 CLI는 파일 바이트만 읽어 전달한다. `-`는 표준입력. `pem::decode_all`(`crates/qsh-core/src/identity/pem.rs`)로 `CERTIFICATE` 블록 수를 세어 2개 이상(체인·번들)이거나 0개면 `INVALID_ARGUMENT`, `PRIVATE KEY` 블록 혼입도 같은 코드이며 오류 문면과 감사 기록 어디에도 입력 내용을 되비추지 않는다. 계약으로 고정할 실패/무동작 모드 셋: `--cert-file`은 기존 `--fingerprint`와 상호 배타이고 위반은 `INVALID_ARGUMENT`(ADR-0013 결정 4 `:23`); 이름이 이미 다른 fingerprint로 pin돼 있으면 `--cert-file` 경로도 `--fingerprint`와 같이 무동작이고 `created=false, updated=false`로 성공을 반환한다(`crates/qsh-core/src/trust/mod.rs:230-233`) — 이것을 `INVALID_ARGUMENT`로 만들면 계약 위반이다; `--cert-file`은 유효기간을 검사하지 않고 만료는 handshake에서 강제된다. ④ `trust add-ca` — `TrustStore::add_ca`(`crates/qsh-core/src/trust/mod.rs:278`, `qsh cert issue`가 자기 CA 루트를 등재할 때 쓰는 함수)를 호출하되 그대로 두지 않는다. ADR-0013 결정 5(`:24`)가 명시한 대로 등재 전에 "같은 이름 + 다른 `cert_pem` → `INVALID_ARGUMENT`" 가드를 `Ops` 쪽에 얹고(같은 이름 + 같은 PEM은 멱등), 그 함수의 정당화 doc 주석(`trust/mod.rs:272-277`, 제자리 덮어쓰기를 "로컬 CA 재-init 때만 생긴다"로 정당화)을 `cert issue`의 로컬 재-init 경로 한정으로 좁혀 다시 쓴다. 구조 검증은 결정 4와 같은 규율 — `decode_all`로 `CERTIFICATE` 블록 수가 정확히 하나가 아니면 거부하고, 등재 전에 DER을 X.509로 파싱해 실패하면 같은 코드로 거부한다(체인 검증은 하지 않는다, self-signed 단일 인증서). 등재 시점에 검증하지 않으면 `TrustStore::parsed_cas`(`trust/mod.rs:319`)가 로드 시점에 `WARN` 한 줄만 흘려 CA 경로가 조용히 죽는다. `<name>`은 principal이 아니라 `[[ca]]` 라벨이다. 쓰기는 `TrustStore::lock`이 덮는 read-modify-write 안에서(M7 Step 7-1이 세운 잠금 — 호출 지점 `ops/mod.rs:548`·`:575`·`:699`). ⑤ ACL — 두 신규 op 모두 로컬 operation이다. `docs/CLI.md:116`(§2.5 마지막 행)은 이미 와일드카드 `trust.*`를 쓰므로 `trust.add_ca`는 토큰을 더할 필요가 없고, ADR-0013 결과(`:36`)가 명시적으로 요구한 `identity.export`만 그 행에 이름으로 추가한다(오늘 그 행은 `identity.init`만 열거한다). `OP_REGISTRY` row는 0이다. ⑥ 문서 — CLI.md §6.11에 "초대 상환이 `qsh serve` 전용이라 `qsh listen` 상대로는 코드 pairing이 안 된다"는 문장과 그 대체 경로(이 ADR의 파일 교환)를 명시하고, §6.13에서 같은 한계를 상호 참조한다. 시나리오 B의 본문은 두 명령의 실제 호출 두 줄(양방향이므로 왕복 네 줄)로 CLI.md와 README에 싣는다. README `Known limitations`에 "listener는 코드 pairing 대상이 아니다" 한 줄(ADR-0013 결과 `:38`). PRD §6(신뢰 설정 경로 열거에 파일 교환 추가)과 §11(명령 체계 표에 `qsh identity export`·`trust add-ca` 행).

**(b) 건드릴 파일:** `crates/qsh-proto/src/types.rs`(`IdentityExportData`, `TrustAddCaData`, `TrustAddReq.cert_pem`), `crates/qsh-proto/src/schema.rs:23-50`(`CLI_V1_SCHEMA_COMMANDS` 26개에 2건 정렬 삽입)·`cli_v1_data_schema` arm, `crates/qsh-core/src/ops/mod.rs`(+ `ops/identity.rs` 신설 후보), `crates/qsh-core/src/trust/mod.rs`(`add_ca` doc 주석 `:272-277` 포함), `crates/qsh-core/src/identity/mod.rs`·`identity/pem.rs`(PEM 검증 재사용), `crates/qsh-cli/src/cli.rs`(`Command::Init` 부근 + 신설 `Command::Identity`, `TrustCmd::AddCa`, `TrustAddArgs.--cert-file` — `:549-564`), `crates/qsh-cli/src/main.rs`(dispatch + `command_name`), `crates/qsh-cli/src/render/human.rs`(현재 `pub fn print_*` 28개 — 신규 2), `crates/qsh-testkit/src/fixtures.rs`(`normalize`에 `cert_pem` 마스크), `crates/qsh-cli/tests/fixtures.rs`, `crates/qsh-core/tests/acl_registry.rs`(§2.5 행 토큰 + `trust.*` 전개), `docs/CLI.md` §6.11·§6.13·§2.4·§2.5, `docs/PRD.md` §6·§11, `README.md`, `docs/man/`.

**(c) 테스트:** ① 등록 완전성 테스트 신설(DoD 5, `crates/qsh-core/tests/acl_registry.rs` 형태). op 등록 3계층 + single-source 가드로 세운다.

- Layer 1: `docs/CLI.md` §2.4 op 목록(`:65-91`, 실측 27종)과 `Operation` impl 전수(`grep 'const COMMAND' crates/qsh-core/src/ops/` = 28건 중 트레이트 선언 1건을 뺀 27건)의 양방향 set-equality. `crates/qsh-core/tests/schema_commands_registry.rs`가 `crates/qsh-core/src/ops/` 소스 스캔으로 그중 두 면을 이미 덮으므로(`:29-35`, `:83-98`) 중복 없이 §2.4 축만 확장한다. `session.attach`는 stream operation이라 §2.4엔 있고 `CLI_V1_SCHEMA_COMMANDS`엔 없다 — 기존 `EXCLUDED`(`schema_commands_registry.rs:126-133`)와 같은 사유 명시 목록으로 승계하고, data 스키마 면 검사에서 사유와 함께 제외한다.
- Layer 2: 신규 op마다 6면이 존재. schemars 타입 / `cli_v1_data_schema` arm / `CLI_V1_SCHEMA_COMMANDS` 등록 / 렌더러 human·JSON / CLI.md 문서 행 / `docs/man/` 파일. JSON 렌더러 면은 관측 가능한 형태로 재정의한다 — `crates/qsh-cli/src/render/json.rs`에는 op별 분기가 없고 `Envelope::success`/`failure`/`print`/`print_event` 넷(`:22`, `:34`, `:52`, `:65`)에 제네릭 `Serialize` 경로뿐이므로 "분기 존재"를 볼 대상이 없다. 대신 (가) 그 op의 golden fixture가 존재하고 `command` 필드가 dotted 이름과 일치, (나) `main.rs` dispatch에 `Envelope::success(<Op>::COMMAND, …)` 호출이 있음을 소스 스캔(`acl_registry.rs:1114-1223`의 `source_scan` 선례)으로 확인한다. man 면은 dotted 이름에서 파일명을 유도할 수 없으므로(실측: `identity.init`의 man은 `qsh-init.1`, `host.list`는 `qsh-hosts.1`) 명시적 `(op, man 파일)` 표를 두고 표 자체의 전수성을 `CLI_V1_SCHEMA_COMMANDS`로 게이트한다 — 표에 빠진 op = FAIL, 표에만 있고 파일 없음 = FAIL.
- Layer 3: 인가 불요 op이 §2.5 exclusion 목록에 사유와 함께 등재. `crates/qsh-core/tests/acl_registry.rs:254`가 `trust.*`를 `["trust.add", "trust.list", "trust.remove"]` 하드코딩으로 전개하므로, 신규 trust op을 여기 더하지 않으면 테스트는 붉어지지 않고 exclusion 커버리지만 조용히 줄어든다. 즉 "고정 계수가 안 움직인다"가 곧 "검증된다"는 뜻이 아니다. 그 전개 목록을 `trust.add_ca`(Step 7에서 `trust.rename`) 포함으로 갱신하고, §2.5 행 토큰에 `identity.export`를 더한다. 무변경이어야 하는 것은 `OP_REGISTRY` row 수·`Body` variant 수 18(`:373`)·`NO_AUTHORIZATION_NEEDED_REASON_COUNT = 7`(`:368`)·`DISPATCH_DRIVABLE_OPS.len() == 10`(`:1068`)·`TESTKIT_ONLY_OPS` 3(`:1089`)이다. 그중 하나라도 움직여야 한다면 그 op이 인가를 필요로 한다는 뜻이므로 판정을 다시 한다.
- single-source 가드: `acl_registry.rs:1081-1104`의 `single_source` 모듈(`DISPATCH_DRIVABLE_OPS ∪ TESTKIT_ONLY_OPS == OP_REGISTRY`)과 같은 모양.

② fixture 결정성 — `qsh_testkit::fixtures::normalize`가 마스킹하는 키는 열거된 목록뿐이고(`crates/qsh-testkit/src/fixtures.rs:73-170`) `cert_pem`은 없다. identity 키 재료는 매 실행 새로 생기므로 `IdentityExportData.cert_pem`을 그대로 두면 `identity.export.json` golden이 회차마다 달라진다. `normalize`에 `cert_pem` 마스크를 추가한다 — 기존 fixture 중 `cert_pem` 필드를 가진 것이 0건임을 실측으로 확인했으므로(`grep -rl cert_pem crates/qsh-cli/tests/fixtures/` 결과 없음) 이 공유 코드 변경은 기존 43개의 바이트를 바꾸지 않는다. `docs/design/testing.md`의 "payload가 자라는 표면은 golden 대신 구조 동등성" 선례를 쓰는 대안은 택하지 않는다 — DoD 5의 "신규 op마다 새 JSON fixture"(`docs/ROADMAP.md:125`)와 충돌하기 때문이다(§4.1 #14).

③ 신규 fixture 3 — `identity.export.json`, `trust.add.cert_file.json`, `trust.add_ca.json`을 `REQUIRED_FIXTURES`(`fixtures.rs:77-121`, 실측 43개)에 등재하고 기존 43개는 byte 무변경.

④ PEM negative 표를 3열로 갈라 쓴다.

| `INVALID_ARGUMENT` | 성공 무동작 | 검사하지 않음 |
|---|---|---|
| `CERTIFICATE` 2블록 이상(체인·번들), 0블록, `PRIVATE KEY` 혼입, 빈 파일, X.509 파싱 실패, `--cert-file` × `--fingerprint` 동시 지정, `trust add-ca` 같은 이름 + 다른 PEM, `identity export --out`이 기존 파일과 겹침 | 이름이 이미 다른 fingerprint로 pin됨 → `created=false, updated=false` 성공, `trust add-ca` 같은 이름 + 같은 PEM(멱등) | leaf 유효기간(만료된 leaf도 통과, 만료는 handshake가 강제) |

별도로 `CONFIG_ERROR` 1건 — identity 부재 시 `identity export`가 "no local identity; run `qsh init` first". `-`(stdin) 경로 1건.

⑤ e2e 1건 — `identity export`로 뽑은 PEM을 상대가 `trust add --cert-file`로 먹여 실 QUIC handshake가 서는 것(ADR-0013 결정 1이 1급 경로로 승격한 것의 증거, `crates/qsh-cli/tests/cert_e2e.rs` 선례). ⑥ `--json`에서 원시 PEM이 stdout 비-JSON으로 새지 않고 `--out` 사용 시 두 모드 모두 stdout에 PEM이 없음(`jsonl_purity.rs`).

**(d) 완료 판정:** 등록 완전성 테스트가 6면 중 어느 하나를 지웠을 때 FAIL(6/6 mutation), 신규 fixture 3 등재·기존 43 무변경, `normalize`의 `cert_pem` 마스크 추가가 기존 fixture 바이트를 바꾸지 않음을 전 fixture 대조로 확인, `CLI_V1_SCHEMA_COMMANDS` 정렬 테스트(`schema.rs:114`)와 arm 대조(`:104`) green, negative 3열 전건 green, e2e green, `add_ca` doc 주석이 `cert issue` 한정으로 갱신, CLI.md §6.11·§6.13·§2.4·§2.5 행과 PRD §6·§11과 README 두 줄과 man 3면(`qsh-identity.1`·`qsh-identity-export.1`·`qsh-trust-add-ca.1` — ADR-0013 결과 `:36`이 그룹 페이지도 요구하고 `docs/man/`에 `qsh-cert.1`·`qsh-trust.1`·`qsh-acl.1` 같은 그룹 페이지가 실제로 있어 관례가 살아 있다)이 같은 커밋.

### Step 7 — 이름 결정권 이동(`pair … --as`) + `trust rename` (S7, 0.5ew)

근거 ADR: ADR-0012 결정 3(명령 표기 `pair invite [--as N]` / `pair accept <address> <code> [--as N]` — `0012-human-surface-naming.md:27`), 결정 6(이름 결정권은 pin하는 쪽, wire 자칭 값은 `device_id` 그대로, `TrustAcceptReq`/`TrustInviteReq`에 additive `as_name`, invite 쪽 `assigned_name` 저장과 상환 시점 검증 — `:30-32`), 결정 4(구 표기 숨김 서브커맨드 — `:28`), 결정 7(`trust rename <old> <new>` — `:35`), 결정 9(dotted 이름 불변 + `accept_command` 값 변경 — `:37`).

선행: Step 5(숨김 서브커맨드 기계와 왕복 테스트 하네스), Step 2(`code` 선택화 — 개명 대상 표면이 이미 그 모양), Step 3(페어링 고지 상수와 그 축자 테스트 — 이 스텝이 개정한다), Step 6(등록 완전성 테스트).

**(a) 범위:** ① `pair invite`/`pair accept`를 CLI 표기로 신설하고 구 `trust invite`/`trust accept`(`crates/qsh-cli/src/cli.rs:468-484`)를 숨김 서브커맨드로 v1 내내 유지한다. ② dotted op 이름은 `trust.invite`/`trust.accept`를 유지한다. `docs/CLI.md:94`가 "CLI subcommand 표기와 이 dotted 이름은 서로 다른 계층이며, envelope의 `command` field와 audit record는 항상 이 dotted 이름을 사용한다"고 이미 두 층을 갈라 놓았고 ADR-0012 결정 9(`:37`)가 4개 dotted 이름의 불변을 못박았다. 유지하면 `CLI_V1_SCHEMA_COMMANDS`의 `"trust.invite"`/`"trust.accept"`(`crates/qsh-proto/src/schema.rs:23-50`), `cli_v1_data_schema` arm, 기존 fixture `trust.invite.json`/`trust.accept.json`, audit record가 전부 무변경이다(§4.1 #7). ③ `accept_command` 값 변경 — ADR-0012 결정 9(`:37`)가 `TrustInviteData.accept_command`가 렌더하는 값을 `qsh trust accept <address> <code>`에서 `qsh pair accept <address> <code>`로 바꾸기로 정했고, 그 문자열을 만드는 유일한 지점은 `crates/qsh-core/src/ops/mod.rs:615`의 `format!("qsh trust accept <address> {code}")`다. 같은 커밋에서 함께 움직일 것 넷: `crates/qsh-cli/tests/trust_pairing_live.rs:37`의 `accept_command.starts_with("qsh trust accept <address> ")` prefix 단언, `crates/qsh-cli/src/cli.rs:473`의 `TrustCmd::Invite` doc("printed `qsh trust accept` command line"), `crates/qsh-core/src/doctor.rs:183`의 `PEER_UNTRUSTED.remedy`("pair with `qsh trust invite` / `qsh trust accept`."), `README.md:214`·`:217` 예제. fixture는 안전하다 — `crates/qsh-cli/tests/fixtures/cli-v1/trust.invite.json`이 이 값을 `"<accept_command>"`로 redact하고 `crates/qsh-testkit/src/fixtures.rs:89-90`이 마스킹하므로 ADR의 "fixture 편집이 아니다"가 맞다. ④ `--as N` — `TrustInviteReq`(`crates/qsh-proto/src/types.rs:643`, 현재 빈 구조체이고 자기 doc이 "미래 optional 파라미터는 additive"라고 적어 둔 자리)에 `as_name: Option<String>`, `TrustAcceptReq`(`:742-751`)에 `as_name: Option<String>`. `TrustInviteData`(`:647`)에 `assigned_name: Option<String>`을 `#[serde(default, skip_serializing_if = "Option::is_none")]`으로 더해 부재 시 키가 생략되게 한다 — 기존 `trust.invite.json` fixture가 byte-identical로 남는 조건이다(`TrustAcceptData.updated`가 같은 방식으로 이미 additive로 들어간 선례, `:755-766`). accept 결과의 이름은 기존 `TrustAcceptData.peer.name`이 그대로 담고 새 echo 필드는 두지 않는다. invite 쪽은 `InviteRecordFile`(`crates/qsh-core/src/trust/pairing.rs`)에 optional `assigned_name`을 신설하고 상시 `qsh serve` 안의 `crate::pairing::respond`가 상환 시점에 `try_pin(&proof_msg.device_name)` 대신 그 값(있으면)을 pin한다. 라벨 검증은 셋이 공유하는 함수 하나로 둔다 — 제어문자 거부(`crate::pairing::reject_control_chars`), 빈 값 거부, `Principal` 세그먼트 형태(`crates/qsh-transport/src/identity.rs`) 만족, 위반은 `INVALID_ARGUMENT`. 파일에서 읽어 온 `assigned_name`도 상환 시점에 같은 검증을 다시 통과해야 하고 실패하면 상대 자칭 값으로 폴백하지 않고 상환 자체를 거부한다(폴백은 파일 손상 한 번으로 이름 결정권이 상대에게 넘어가는 다운그레이드다). 충돌 판정 대상도 `--as`/`assigned_name`이 주어지면 그 값이다. ⑤ 페어링 고지 개정 — Step 3이 "이 이름은 상대가 자칭한 값이다" 단서를 포함해 세운 문면(`crates/qsh-core/src/server/mod.rs:2778`의 pin 콜백)을 `--as` 착지 후 문면으로 다시 쓰고, Step 3이 심은 축자 테스트도 같은 커밋에서 갱신한다(ADR-0017 결정 3 `:32`). ⑥ wire 무변경 — `PairingProof.device_name`/`PairingAccepted.device_name`은 오늘처럼 각자의 `device_id`가 나간다(ADR-0012 결정 6). `docs/design/protocol.md` §15의 wire 변경 0, M8 freeze(§16) 무저촉. ⑦ `trust rename <old> <new>` — `TrustStore`에 rename을 신설한다(현재 `add_peer`/`remove`/`add_ca`만, `crates/qsh-core/src/trust/mod.rs:224-297`) + `Ops::trust_rename`(신규 op 1, `trust.rename`). `new`가 이미 다른 항목의 이름이면 `SESSION_CONFLICT`로 실패하고 병합하거나 조용히 덮어쓰지 않는다. 이미 붙어 있는 연결은 rename 전 권한을 유지한다. audit 기록은 `crates/qsh-core/src/audit.rs`의 sink를 부른다 — 로컬 config 조작이 `audit.log`에 남는 첫 사례다(ADR-0012 결정 7). "재시작 불요"는 `trust.toml`이 매 handshake마다 내용 기반으로 재로드되므로(`docs/CLI.md:689`) 새 로직 없이 성립할 가능성이 높다 — 실측으로 확인하고 성립하면 그 사실을 테스트로 고정한다. `acl.toml`은 hot reload가 없어(`crates/qsh-core/src/acl/load.rs`) 새 이름을 겨눈 매칭 규칙이 재시작 전까지 없고 그 사이는 default-deny로 fail closed다. ⑧ README 델타 중 이 스텝 몫 — 페어링 예제 전부와 `accept_command` 예제 값(`README.md:47`, `:212`, `:214`, `:217`, `:416`, `:598`, `:607`).

**(b) 건드릴 파일:** `crates/qsh-proto/src/types.rs`, `crates/qsh-proto/src/schema.rs`(`trust.rename` 1건만 추가), `crates/qsh-core/src/ops/mod.rs`(`:615` `accept_command` 포함), `crates/qsh-core/src/trust/mod.rs`·`trust/pairing.rs`(`InviteRecordFile.assigned_name`), `crates/qsh-core/src/pairing.rs`(`respond`의 `try_pin`), `crates/qsh-core/src/server/mod.rs`(고지 문면 개정), `crates/qsh-core/src/doctor.rs`(`PEER_UNTRUSTED.remedy`), `crates/qsh-core/src/audit.rs`(호출만), `crates/qsh-cli/src/cli.rs`(`PairCmd` + hide된 `TrustCmd::Invite`/`Accept` + `TrustCmd::Rename` + `:473` doc), `crates/qsh-cli/src/main.rs`, `crates/qsh-cli/src/render/human.rs`, `crates/qsh-cli/tests/fixtures.rs`, `crates/qsh-cli/tests/trust_pairing_live.rs`, `crates/qsh-core/tests/acl_registry.rs`(`trust.*` 전개에 `trust.rename` 추가), `docs/CLI.md` §6.11·§2.4·§2.5, `docs/PRD.md` §11·`:336`, `docs/design/protocol.md` §15(이름 결정권이 CLI/JSON 축이고 wire 자칭 값은 불변이라는 1줄), `README.md`, `docs/man/`.

**(c) 테스트:** 숨김 서브커맨드 왕복(DoD 4) — `trust invite` ≡ `pair invite`, `trust accept` ≡ `pair accept`를 파싱 층과 dispatch 층 두 곳에서. `--help`에 구 이름이 안 보이는 것 1건. `accept_command` — 값이 `qsh pair accept` 표기를 담고 `trust.invite.json`은 byte-identical임을 단언(`trust_pairing_live.rs:37` prefix 단언을 새 값으로 갱신). `--as` 왕복 — invite 쪽이 정한 이름이 상환 시 pin되고 accept 쪽 `--as`는 자기 store의 이름을 정한다(두 축 독립), 실 QUIC pairing e2e(`trust_pairing_live.rs` 확장). 라벨 검증 negative — 제어문자·빈 값·`Principal` 세그먼트 위반은 `INVALID_ARGUMENT`, 손상된 `assigned_name`은 상대 자칭 값으로 폴백하지 않고 상환을 거부. 충돌 — `--as`/`assigned_name`이 주어지면 자칭 값이 아니라 그 값을 대상으로 `SESSION_CONFLICT`/`PinCollision`. wire 무변경 실증 — pairing 프레임의 `device_name`이 여전히 `device_id`임을 단언하고, `as_name`을 wire에 싣는 mutation이 이 테스트를 FAIL시킨다. 페어링 고지 — Step 3이 세운 축자 테스트가 새 문면으로 갱신되고 "자칭 값" 단서가 사라진 것을 단언. `trust rename` — 다음 handshake부터 적용(실 QUIC, `crates/qsh-cli/tests/trust_lifecycle_live.rs` 선례), `new` 충돌 `SESSION_CONFLICT`, audit record 정확히 1건(`MemoryAuditSink` 사용 — `crates/qsh-core/tests/acl_registry.rs:433`이 그 import 선례), 기존 연결 권한 유지, `acl.toml` 미반영 구간의 default-deny, `TrustStore::lock` 경유 확인. fixture — `trust.invite.json`/`trust.accept.json` byte 무변경 + 신규 `trust.rename.json`·`trust.invite.assigned_name.json` 등재. 등록 완전성 테스트가 `trust.rename` 6면을 요구.

**(d) 완료 판정:** 위 전부 green, `crates/qsh-proto`의 wire 모듈 diff 0 실증, audit 1건 단언, 기존 fixture 무변경, `accept_command` 값 변경에 딸린 네 지점(`trust_pairing_live.rs:37`, `cli.rs:473`, `doctor.rs:183`, `README.md:214`·`:217`)이 같은 커밋에서 갱신, `PEER_UNTRUSTED.remedy` 개정이 Step 3의 8종 문면 게이트와 충돌하지 않음 확인(이 문면은 8종에 없다), Step 3이 세운 페어링 고지 축자 테스트가 새 문면으로 갱신됨, CLI.md §6.11의 `pair` 절·§2.4(개명에도 dotted 이름을 유지하는 사유 1줄 + `trust.rename` 행)·man 항목이 같은 커밋.

### Step 8 — `qsh service install|uninstall|status [--json]` (S8, 0.4ew)

근거: 이 항목은 근거 ADR이 지정되지 않았다. 스펙 정본은 `docs/ROADMAP.md:118` (g) 본문이고, 유예 가드레일에서 M9로 승격된 기록은 `:160`이다. ADR-0012 결정 8(`0012-human-surface-naming.md:36`)은 둘째 터미널 마찰의 해법으로 `qsh service install` 안내를 들지만 unit 경로·모드 추론을 정하지 않는다(`serve --pair` 기각 근거 쪽이다 — ROADMAP Q5, `:133`). 모드 추론이 config를 읽는 방식만 ADR-0012 결정 5(`:29`)를 재사용한다.

선행: Step 4(unit 경로 상수와 모드 추론 순수 함수), Step 5(`[serve].to` + 충돌 `CONFIG_ERROR`), Step 6(등록 완전성 테스트), §8 열린 질문 2·3(unit 라벨 토큰, plist 정본)·5(Windows `UNSUPPORTED` 문면).

**(a) 범위:** ① 신규 op 3 — `service.install`, `service.uninstall`, `service.status`(이름 §4.1 #6). `Command::Service(ServiceCmd)` 신설, 로직은 `crates/qsh-core/src/ops/service.rs` 신설(현 `ops/` 목록에 없음). ② 모드 추론 — `[serve].to`(또는 구 `[reverse].controller`)면 `serve --to` 모드, `[listen]`만 있으면 `listen` 모드, 아니면 `serve` 모드(`docs/ROADMAP.md:118`). 두 config 키의 값이 다르면 `CONFIG_ERROR`(ADR-0012 결정 5가 `service install`을 명시적으로 그 쪽에 넣었다). `<mode>` 토큰 값은 열린 질문 2의 답을 Step 4가 격리해 둔 상수에 채운다. ③ macOS는 사용자 LaunchAgent(`~/Library/LaunchAgents/io.qsh.<mode>.plist`, `KeepAlive`), Linux는 systemd user unit(`~/.config/systemd/user/qsh-<mode>.service`, `Restart=always`), 그 외·매니저 없음은 `UNSUPPORTED`. 템플릿의 정본은 `docs/deploy/service.md:17-42`(launchd serve)·`:106-135`(systemd)이다(열린 질문 3이 다른 답을 내면 그 답이 정본). ④ Windows는 세 모드 모두 unit을 쓰기 전에 `UNSUPPORTED`를 낸다 — 파일 쓰기 0을 테스트로 단언한다. ⑤ unit 인자는 고정이고 qsh 자체 데몬화는 바꾸지 않는다(foreground 전용, `docs/CLI.md:765`). ⑥ Step 4의 "서비스 미등록" 진단이 보는 경로와 install이 쓰는 경로가 같은 상수임을 테스트로 고정한다. ⑦ `docs/deploy/service.md:190-194`의 forward-note("`qsh service install` is planned for M9", "`qsh reverse`도 `qsh serve --to`로 개명 예정")를 실현 문면으로 고친다. ⑧ fixture 결정성 — unit 경로가 테스트별 `HOME` tempdir 아래이므로 그 필드를 정확히 `path`로 명명해 기존 `normalize` 마스크(`crates/qsh-testkit/src/fixtures.rs`의 `"path"` arm)에 걸리게 한다. 다른 이름을 쓰면 Step 6과 같은 `normalize` 확장이 필요하다(§4.1 #14).

**(b) 건드릴 파일:** `crates/qsh-core/src/ops/service.rs`(신설), `crates/qsh-core/src/ops/mod.rs`(`Operation` impl 3), `crates/qsh-core/src/doctor.rs`(경로 상수 공유), `crates/qsh-proto/src/types.rs`·`schema.rs`, `crates/qsh-cli/src/cli.rs`·`main.rs`·`render/human.rs`, `crates/qsh-cli/tests/fixtures.rs`, `crates/qsh-core/tests/acl_registry.rs`(§2.5 행에 `service.*` 3 토큰), `docs/CLI.md`(§6 신설 절 + §2.4 + §2.5), `docs/deploy/service.md`, `docs/man/`.

**(c) 테스트:** 파일시스템 격리 — `HOME`을 테스트별 tempdir로 주입해 실제 `~/Library/LaunchAgents`·`~/.config/systemd/user`를 건드리지 않는다(`docs/design/testing.md:135-144`의 테스트별 tempdir 규율). 서비스 매니저 실호출(`launchctl`/`systemctl`)은 하지 않는다 — §3 non-goal. install → status → uninstall 왕복(파일 존재·부재), 같은 모드 재install 멱등, 모드 추론 4조합, config 값 충돌 `CONFIG_ERROR`, Windows leg에서 `UNSUPPORTED` + 생성 파일 0(`cargo check --target x86_64-pc-windows-gnu` 크로스 체크 포함 — M7 Step 3·Step 5가 Windows leg에서 두 번 붉어진 자리다). 신규 fixture 3(`service.install.json`, `service.status.json`, `service.uninstall.json`) 등재 + 등록 완전성 6면 × 3 op. `docs/deploy/service.md`의 예시와 생성물이 같은 모양임을 문서-코드 대조로 고정(`doctor_docs.rs` 형태). `jsonl_purity.rs` 회귀.

**(d) 완료 판정:** 왕복·멱등·추론·Windows 네 축 green, 테스트가 tempdir 밖 파일을 만들지 않음을 단언, Step 4의 경로 상수 공유 테스트 green, 신규 fixture 3이 회차 간 결정적, `service.md`·CLI.md 신설 절·§2.4·§2.5·man 4면(`qsh-service.1` 그룹 + `qsh-service-install.1`·`qsh-service-uninstall.1`·`qsh-service-status.1`)이 같은 커밋.

### Step 9 — 계약 문서 횡단면 반영 (S9, 0.4ew)

근거: `docs/ROADMAP.md:127`의 "S9 계약 문서(CLI.md·PRD.md·man) 반영 0.4 — README 전면 재작성 제외", 마감 공통 절차 2(`:29`). 선행: Step 1~8 전부(각 스텝이 자기 행과 자기 README 델타를 이미 실었다).

**(a) 범위:** 각 스텝이 자기 CLI.md 행·man 항목·fixture·스키마 등록·README 델타를 같은 커밋에 넣었으므로 여기 남는 것은 횡단면뿐이다. ① CLI.md §2.4 op 레지스트리 목록(`docs/CLI.md:65-91`, 현재 나열 27종)에 신규 6 op(`identity.export`, `trust.add_ca`, `trust.rename`, `service.install`, `service.uninstall`, `service.status`)이 빠짐없이 들어가 33종이 됐는지 최종 정합, `:98`의 "`qsh serve`/`qsh listen`/`qsh reverse`는 operation이 아니라 장기 실행 모드" 문장에 `serve --to` 반영. ② CLI.md §2.5 매핑 표 최종 정합 — Step 6이 `identity.export`, Step 8이 `service.install`/`service.uninstall`/`service.status`를 "인가 불요" 행(`:116`)에 넣었는지 확인한다. `trust.add_ca`·`trust.rename`은 기존 와일드카드 `trust.*`가 이미 덮으므로 토큰을 더하지 않는다(ADR-0012 결과 `:42`가 "매핑 표는 무변경"이라 적은 것이 이 뜻이다). 같은 결과 절이 요구한 표 아래 한 줄("인가 대상이 아니라는 뜻이지 부작용이 없다는 뜻이 아니다")이 서 있는지 확인한다(Step 5 ⑦ⓒ 소유). ③ `docs/CLI.md:3` 상태 헤더 갱신(현재 "Draft v0.10 (M3 Step 8 …)" — M8 마감이 한 줄 개정했다고 적었으므로 M9 착수 시 실측 후 갱신). ④ `docs/PRD.md` 최종 정합 — §11 명령 요약(`:241-257`)에 `qsh pair`·`qsh service`·`identity export`·`trust add-ca`·`trust rename`, `:80`의 신뢰 설정 문장에 인증서 파일 교환 경로 병기, `:336`의 pairing UX 확정 결정에 code 선택화·`--code-stdin` 반영. ⑤ `docs/design/testing.md`에 M9 문단 — 계층별로 무엇이 새로 서는지 서술하고(L6가 "레버리지가 가장 큰 계층"이라는 `:79` 서술에 M9의 새 행이 붙는다), 값-보유 golden 규율과 등록 완전성 게이트의 채택 기록을 `:88` 형식으로 남긴다. `normalize`에 `cert_pem`이 추가된 사실과 그 사유도 같이 적는다. doctor는 golden fixture를 byte-freeze하지 않는다는 M7 판정을 M9 신규 7종에도 그대로 적는다. ⑥ `docs/design/architecture.md`·`docs/design/protocol.md` 정합(wire 무변경이므로 protocol.md는 §15에 1줄; architecture.md §7 config 절은 Step 5 ⑦ⓔ 소유이고 여기서는 확인만). ⑦ README 잔여 횡단면 — 기능 목록·인터임 위험 고지. 역할 표·역방향 예제는 Step 5, 페어링 예제·`accept_command`는 Step 7, Known limitations 두 줄(포트 공유 = Step 3, listener 코드-pairing 부재 = Step 6)이 이미 갚았다. 여기서는 그 델타가 서로 모순 없이 한 문서로 읽히는지만 본다. 서사·구조 전면 재작성은 `docs/ROADMAP.md:119` 명시적 out. ⑧ DoD 1의 캠페인 문서 사전 정의 — `docs/campaigns/m9-stopwatch.md`를 `m7-stopwatch.md` 형식(사전 고정된 기준, 회차 기록 표, 측정을 사람이 해야 하는 사유)대로 낸다. 측정 3회는 사람 몫이고 Step 11 이전에 수행된다. ⑨ man 전량 재생성 1회(`cargo run -p xtask -- man`). ⑩ 이월 항목 — `crates/qsh-core/src/resume.rs:320`의 `purge_expired` doc이 HEAD부터 틀리다("Called by the same reaper pass…") — 주석 정정(§6 행 iii).

**(b) 건드릴 파일:** `docs/CLI.md`, `docs/PRD.md`, `docs/design/testing.md`, `docs/design/architecture.md`, `docs/design/protocol.md`, `README.md`, `docs/campaigns/m9-stopwatch.md`(신설), `crates/qsh-core/src/resume.rs`(주석), `docs/man/` 전량.

**(c) 테스트:** `acl_registry.rs`의 §2.5 양방향 대조(`:122`)와 `trust.*` 전개(`:254`), Step 6이 신설한 등록 완전성 테스트, `doctor_docs.rs`의 CLI.md 산문 계수(`:150`)와 code 이름 게이트(`:124`), man byte-exact(`xtask/src/man.rs:93-124`), `acl_docs.rs`의 PRD §9 액션 어휘 대조(`:133`)가 신규 op 추가에 흔들리지 않음(신규 op는 ACL action을 더하지 않는다).

**(d) 완료 판정:** §2.4 33종·§2.5·CLI.md:3·PRD §11·testing.md M9 문단·README 잔여면이 한 커밋, 게이트 6종 green, 캠페인 문서가 실행 전에 기준을 고정한 상태로 존재, 이 스텝에서 추가된 CLI.md 행이 횡단면뿐(스텝별 커밋에서 등록 완전성 테스트가 이미 green이었다는 사실이 그 증거).

### Step 10 — ADR 확정 반영과 예약 ADR 갱신 (S10, 0.3ew)

근거: `docs/ROADMAP.md:127` — "S10 ADR 0.3: 0012·0013·0014·0017은 확정 반영, 0015·0016 신규 작성". 선행: Step 1~9(ADR이 미룬 값들이 구현에서 확정된 뒤).

**(a) 범위:** ① 실측 상태(`docs/adr/README.md:7-24`): 0012 승인됨, 0013 승인됨, 0014 제안됨(파일 헤더 `0014-address-default-port.md:4`도 같다), 0015 예약됨, 0016 예약됨, 0017 승인됨, 0018 승인됨. ADR-0014의 `제안됨` → `승인됨` 전환은 §0.1의 사용자 확정으로 Step 1 착수 전에 이미 끝난다 — 이 스텝이 하는 일은 그 확정된 상태를 본문과 색인에 반영하고 Step 1이 착륙시킨 실제 동작(정규화 함수의 최종 배치, 순수 IPv6 리터럴 처리, peer 주소 포트 범위 처분)으로 본문을 맞추는 것뿐이다. 구현을 근거로 상태를 올리지 않는다. ② ADR이 명시적으로 미뤄둔 값들을 결과 절에 기입한다 — ADR-0012 결과 절(`:43`)이 "code 문자열을 이 ADR이 확정하지 않는다"고 남긴 자리에 Step 5의 config 충돌 code와 status를, ADR-0017 결과 절(`:46`)이 ADR 밖으로 넘긴 doctor 4종의 code·status를, ADR-0013 결과 절(`:36`)이 CLI.md §2.4 개정 시점으로 넘긴 `trust.add_ca`/`trust.addca` 표기와 결정 3(`:20`)이 남긴 `qsh init`/`qsh identity init` 관계를 적는다. 열린 질문 6이 (나)로 결정되면 ADR-0012 결과 절의 man 문장을 여기서 개정한다(원문 인용 후 개정문 전문 교체). ③ 0015(listener pairing)·0016(CSR 기반 다대 CA 서명)은 여전히 M9 명시적 out(`docs/ROADMAP.md:119`)이다. 파일은 이미 있고 상태가 `예약됨`(결정 절이 질문 리스트)이므로 S10의 실제 작업은 그 질문 목록을 M9가 확정한 사실에 맞춰 다시 쓰고 아직 미정인 것만 남기는 것이다. 예약 상태 ADR의 서식은 `## 결정`에 "미정. 다음을 정해야 한다." + 번호 매긴 질문, `## 결과`도 "미정."으로 시작, `## 대안`에 채택하지 않은 후보 둘. ④ M9에서 새 번호가 필요한 결정이 나오면 0019부터다(최고 번호는 0018 — `0018-tunnel-lifetime-bound-to-connection.md`). ⑤ 서식·색인 규율: 헤더는 `# ADR-NNNN: <제목>` + `날짜:` + `상태:`, 섹션 순서는 `## 맥락` → `## 결정`(번호 리스트) → `## 결과`(불릿) → `## 대안`(기각 사유 포함), `docs/adr/README.md`의 3열 표(번호·제목·상태)에 번호순 행 추가를 자기 결과 절에 명시(선례 `0012-human-surface-naming.md:42`, `0013-cert-file-exchange.md:39`, `0017-acl-toml-not-written.md:54`). 기존 결정을 개정할 때는 원문 인용 후 개정문 전문 교체(ADR-0013 결정 1이 ADR-0002를 그렇게 개정했다, `:18`).

**(b) 건드릴 파일:** `docs/adr/0012-human-surface-naming.md`, `0013-cert-file-exchange.md`, `0014-address-default-port.md`, `0015-listener-pairing.md`, `0016-csr-issuance.md`, `0017-acl-toml-not-written.md`, `docs/adr/README.md`.

**(c) 테스트:** ADR 상태와 README 색인 표의 일치는 현재 자동 게이트가 없다(실측: `docs/adr/` 대조 테스트 없음). 이 스텝에서 문서 게이트를 신설하지는 않는다 — S10 크기 밖이고, 대조는 Step 11의 절차 1(구속 문서 태그 대조)이 흡수한다.

**(d) 완료 판정:** 0012·0013·0014·0017의 상태와 본문이 착륙한 구현과 일치, 0015·0016이 M9 이후 상태로 갱신, README 색인과 각 파일 헤더 상태가 일치, ADR이 미뤄둔 code·op 이름·표기가 전부 기입됨, 신규 번호는 0019부터.

### Step 11 — 마감 (마감 공통 절차, 크기 내역 밖)

**(a) 선행 감사.** 마감 공통 절차(`docs/ROADMAP.md:25-30`) 두 검사를 §1 DoD가 아직 열린 채로 먼저 돌린다. 파이프라인은 M8 선례대로 인벤토리 → opus 대조 → 반박 검증 → fixer이고 산출물은 `$SP/step11/TAG-AUDIT.md`·`I-readme.md`다.

절차 1(구속 문서 태그 대조, `:27-28`)의 수치 형식: "대조 문장 N건 — 검증 M, 후속 마일스톤 명시 유예 P, 열린 DoD 종속 Q, 대조 대상 아님 R, 충돌 S". M8이 넘긴 열린 DoD 종속 6건(`docs/PRD.md:286`·`:287` = M8 DoD 2, `:307` = M8 DoD 3, `:311` = M8 DoD 4, ADR-0009 `:68` = DoD 3, ADR-0009 `:99` 후반절의 24h soak 판정 = DoD 2 — 출처는 교체 전 `PLAN.md:717`)은 M9 시점에도 M8 소관이므로 그대로 종속으로 남긴다 — M9가 닫는 것이 아니다. 대조 대상 아님에는 예약 상태 ADR(0015·0016 — 결정 절이 비어 있다)이 계속 들어간다.

절차 2(README 동기화, `:29`)의 수치 형식: "대조 항목 N개 중 불일치 M". 서사·구조 전면 재작성은 제외(`:119`)하되, 대조 항목에는 기능 목록·Known limitations·인터임 고지와 함께 ADR이 지정한 델타 넷이 들어간다 — 역할 어휘 표 신설, 역방향·페어링 예제 전부의 신 표기, Known limitations 두 줄(포트 공유 / listener 코드-pairing 부재), `accept_command` 예제 값. 문서가 숨김 서브커맨드를 정규 경로로 광고하고 있으면 그 자체가 불일치다.

**(b) 마감 커밋에 남길 것.** §1 DoD 체크박스, `docs/CLI.md:3` 상태 헤더, `docs/ROADMAP.md:5` "현재 위치", ROADMAP M9 절 갱신 + 마감 노트(절차 1·2 수치, 캠페인 문서와 run id 인용), ROADMAP M9 ✅, §7 태그 정책 한 줄의 ROADMAP 이관, PLAN.md를 M10 계획으로 전면 교체(§3의 P1 재기록 항목과 §6 이월 표의 미종결 행 이관 포함).

**(c) 완료 판정.** §1 DoD 1~7 전부 `[x]`, 절차 1 충돌 0, 절차 2 불일치 0, ROADMAP M9 마감 노트가 `docs/campaigns/m9-stopwatch.md`와 CI run id를 인용, 마감 커밋 CI green. §0.2의 M7·M8 잔여 넷은 마감 조건이 아니다 — 그중 M7 DoD 1만 §1 DoD 1의 선행 조건이다.

### Step 12 — SOCKS `-D` 구현 (ADR-0019, 1.6~2.0ew)

사용자가 2026-09-19에 `-D`를 구현하기로 결정했다. 설계는 ADR-0019가 기록하고 이 스텝은 그 결정을 커밋 순서로 옮긴다. Step 11 마감은 이 스텝이 닫힌 뒤에 한다. 커밋마다 일곱 게이트와 CI acceptance 잡이 초록이어야 한다.

M9가 wire를 건드리지 않는다는 머리말의 원칙에 예외가 하나 생긴다. `StreamHeader`의 필드 5(`deny_host_local`)와 capability `dial-filter.v1`이다. 프레임은 늘지 않고 `docs/design/protocol.md` §16.4에 capability로 보호되는 새 필드 번호를 허용하는 항목을 먼저 넣은 뒤 코드가 들어간다(ADR-0019 결정 4).

**(a) 순서.** 커밋 하나가 아래 항목 하나다.

1. 결정 기록. ADR-0019를 승인된 설계로 교체하고 색인, `docs/ROADMAP.md`, `docs/PRD.md`, 이 절을 고친다.
2. `qsh-proto`의 SOCKS5 codec(`socks5.rs`)과 fuzz 타깃 `parse_socks5`(열여덟 번째). codec은 아직 아무 데서도 부르지 않는다. `docs/design/protocol.md` §13·§16.3을 같은 커밋에서 고친다.
3. §16.4 개정 한 항목만 담은 문서 커밋.
4. host dial 필터. `StreamHeader.deny_host_local`, `dial-filter.v1` 광고, 해석 결과의 host-local 주소 거르기, `TCP_CONNECT` host의 바이트 범주 검사, `capabilities.json` 재생성. client는 아직 필드를 보내지 않는다.
5. client 쪽 SOCKS 드라이버(`qsh-core/src/tunnel/dynamic.rs`). `forward_connection`을 `open_tunnel`과 `splice_opened`로 나누고 listener별 상한(handshake 10초, handshake 슬롯 64, 연결 128, CONNECT 초당 50·burst 100)을 둔다. 속도 초과는 남은 handshake 기한 안에서 기다린다.
6. 역방향 `-D`의 결정 기록. ADR-0020이 ADR-0019 결정 10·13과 Q2를 개정한다. 역방향 controller는 이미 `ControlHandshake.capabilities`로 target의 협상된 capability를 받고, daemon은 `LOCAL_STREAM` 헤더를 바이트 그대로 넘기므로 새 경로를 만들 필요가 없다. 색인과 이 절도 같이 고친다.
7. `Ops::tunnel_dynamic`과 CLI 전환. 거절 상수와 `dynamic_forward_unsupported()`를 지우고 새 op `tunnel.dynamic`, 새 fixture 셋, `RETIRED_PRODUCERS`, `DYNAMIC_FORWARD_ACL_NOTE`, CLI.md §2.5·§3.3·§6.9, README, man 재생성을 한 커밋에 담는다. 역방향 route의 `-D`는 이 커밋에서 아직 `UNSUPPORTED`다. 다음 커밋이 이 거절을 걷으므로 fixture로 고정하지 않고, `UNSUPPORTED`는 `DEFERRED`로 돌아간다(ADR-0020 결정 4).
8. 역방향 `-D`와 대화형 역방향 `-L`. controller가 `ControlHandshake.capabilities`에서 `dial-filter.v1`을 확인하고 `LOCAL_STREAM`의 `TCP_CONNECT` 헤더에 필드 5를 실어 보낸다. 대화형 세션의 forward도 역방향에서는 `tunnel open`과 같은 opener로 연다. `ReverseHarness` e2e로 왕복, target loopback 이름의 `REP 0x02`, capability 부재 거절, 대화형 `-L` 왕복을 고정한다(ADR-0020 결정 1~3).
9. 사실이 바뀐 서술을 고친다. `forward.socks` doc과 `ALWAYS_DENIED_NO_OP` 사유 문면, `docs/design/threat-model.md` §2·§4·§7, `docs/design/architecture.md`, `docs/design/testing.md`, `docs/campaigns/m8-adversarial-load.md`의 두 행이 대상이다. threat-model의 `-D` 행에는 역방향 경로도 넣는다.
10. acceptance. 실제 `curl --socks5-hostname`을 qsh SOCKS listener로 통과시키는 `socks_curl` 테스트를 CI acceptance 잡에 넣는다. 양성 경로, host-local 필터, ACL 거부, 종료 뒤 포트 재bind를 확인한다.

**(a)-추기 — 항목 7·8 착지 (2026-09-21, main 세션).** 항목 7은 dbd7d59, 항목 8은 8dd604d로 main에 올랐다. 8dd604d의 CI(run 35476274774)는 테스트 둘에서 빨갰다. Linux에서는 `$XDG_RUNTIME_DIR`이 잡혀 있어 nextest 프로세스들이 runtime dir 하나를 공유했고, 실제 discovery로 이름을 푸는 역방향 테스트가 다른 테스트의 daemon까지 보고 거절당했다. Windows에서는 대화형 attach가 spec 검사 전에 `UNSUPPORTED`로 끝나 단언이 어긋났다. 제품 코드는 그대로 두고 c4ab4c8이 테스트만 고쳤다(runtime dir 격리와 격리 여부를 고정하는 새 테스트, `cfg(unix)`). c4ab4c8의 CI 35555489133·fuzz-smoke 35555489153·load 35555489142 전부 초록. 항목 9·10이 남았다.

**(b) 완료 기준.**

- `DYNAMIC_FORWARD_UNSUPPORTED`가 저장소에 남지 않는다. retired 사유 문자열과 ADR 본문만 예외다.
- `crates/qsh-cli/tests/fixtures/cli-v1/`의 기존 파일은 한 바이트도 바뀌지 않는다. 재생성은 `capabilities.json` 하나이고 그 diff는 capability 한 줄이다.
- 옛 계약을 고정하던 테스트(`dynamic_forward_stub.rs`, `tunnel_docs.rs`의 네 테스트, `failure_text_discipline.rs`의 T6, `fixtures.rs`의 `-D` 블록)는 새 계약의 테스트로 바뀐다. 자원을 만들지 않는다는 속성은 실패 경로마다 계속 검사한다.
- ACL 테스트의 단언은 바뀌지 않는다. 이름이 바뀌는 테스트는 `is_always_denied_is_exactly_the_p1_deferred_trio` 하나다.
- `socks_curl`이 CI acceptance 잡에서 건너뛰지 않고 초록이다.
- 역방향 `-D`가 켜져 있고, 대화형 세션의 역방향 `-L`도 더는 `UNSUPPORTED`가 아니다.

**(c) 이 스텝이 바꾸는 M9 DoD.** DoD 3의 문구 표본 "`-D` 현행 유지 문구"는 "`-D` 거절 문구(capability 부재)"로 바뀐다. 2026-09-19 판은 역방향 거절도 넣었으나 ADR-0020이 그 거절을 걷었다. 표본 수는 8종 그대로다. DoD 5의 등록 완전성 테스트는 새 op `tunnel.dynamic`에도 걸린다.

## 3. 명시적 non-goals (P1 유예 / 타 마일스톤)

- TOFU(client가 미지 peer를 자동 pin) — `docs/ROADMAP.md:119`. `trust.toml` 양방향 구조상 outbound pin이 상대의 inbound 인증까지 통과시키는 문제가 있어 pin 방향 축을 별도 ADR로 예약(번호 미배정 — ADR-0017 결정 5가 그 자리를 미룬다, `0017-acl-toml-not-written.md:42`).
- listener 대상 초대 상환("listener pairing") — ADR-0015 예약(`docs/adr/0015-listener-pairing.md`, 상태 예약됨).
- CSR 기반 다대 CA 서명 — ADR-0016 예약. 현행 `qsh cert issue`는 로컬 device 승격만 유지(ADR-0008 결정 5).
- README의 서사·구조 전면 재작성 — `docs/ROADMAP.md:119`의 out은 "README 전면 재작성"이고, SC1 baseline 재측정 뒤로 미룬다. ADR이 요구하는 델타는 out이 아니다 — ADR-0012 결정 1·결과(`:25`·`:42`)의 역할 어휘 표와 역방향·페어링 예제와 `accept_command` 예제 값, ADR-0013 결과(`:38`)의 "listener는 코드 pairing 대상이 아니다" 한 줄, ADR-0014 결정 9(`:57`)의 포트 공유 한 줄은 범위 안이고 각각 Step 5·7·6·3이 자기 커밋에서 갚는다. 실측으로 구 표기가 남은 README 줄은 `:34`, `:47`, `:212`, `:214`, `:217`, `:384`, `:391`, `:416`, `:441`, `:533`, `:562`, `:564`, `:598`, `:607`, `:626`이다. README는 축자 게이트 대상이기도 하다 — `readme_quotes_the_controller_unreachable_diagnostic_verbatim`(`crates/qsh-core/tests/doctor_docs.rs:38-47`)이 `CONTROLLER_UNREACHABLE`의 `message`/`remedy`를 verbatim으로 요구한다.
- 서비스 매니저 실호출 — `launchctl bootstrap`·`systemctl --user enable`은 M9가 대행하지 않는다. unit 파일 생성·삭제까지가 범위이고 활성화 절차는 `docs/deploy/service.md:84`·`:141` 문면으로 안내한다.
- qsh 자체 데몬화 — foreground 전용 불변(`docs/ROADMAP.md:118` (g), `docs/CLI.md:765`).
- wire 변경 — M8 freeze(`docs/design/protocol.md` §16) 이후 M9는 CLI/JSON 축만 건드린다. 이름 결정권도 wire 자칭 값을 바꾸지 않는다(ADR-0012 결정 6).
- doctor `--fail-on` — `docs/CLI.md:1072`가 향후 additive 후보로 남긴 플래그. M9 미구현.
- file copy, Windows host — `docs/ROADMAP.md` §3 가드레일 표 그대로. `-D`/SOCKS는 2026-09-19 사용자 결정으로 이 목록에서 빠져 Step 12가 구현한다(ADR-0019).
- `ControlLink`/`DataLink` enum → trait 전환(ADR-0005 P0 부채) — M3부터 연쇄 이월. M9도 트리거하지 않는다. P1 재기록.
- cert rotation/revocation UX — P1(`docs/ROADMAP.md:159`), doctor 만료 경고만.

## 4. 리스크와 감시 항목

- SC1 baseline이 아직 없다. M7 DoD 1이 미실행이라 M9 DoD 1의 "단축 기록"에 비교 대상이 없다. 순서는 `docs/ROADMAP.md:132`가 Q4로 못박았다 — 현행 표면으로 먼저 잰다(Step 0). 새 표면 측정을 먼저 해버리면 DoD 1이 영구히 닫히지 않는다. Step 1이 main에 얹힌 뒤의 baseline은 `525a2a5` 태그 트리 재빌드로만 가능하다.
- 숨김 서브커맨드는 v1 내내 계약이다(ADR-0012 결정 4). 한 번 숨기면 v1 동안 지울 수 없고, 유일한 방어선은 왕복 테스트다. alias 브랜치를 "죽은 코드"로 오인해 정리하는 것이 가장 그럴듯한 회귀 경로인데 컴파일 보호는 절반만이다 — `long_running_setup_mode`(`crates/qsh-cli/src/main.rs:910-917`)는 `_ => None` 때문에 `Reverse` arm 삭제가 컴파일로 잡히지 않고, 그렇게 되면 `qsh reverse`가 장기 실행 모드 오류 경로 대신 JSON envelope 경로로 조용히 빠진다. 이쪽만 테스트로 막아야 한다. `command_name`(`:1190-1225`)은 와일드카드 없는 완전 match라 컴파일이 잡는다.
- doctor 동결 set이 14 → 21로 절반 늘어난다. `EXPECTED_DOCTOR_CODES`, `DiagnosticId`, `doctor.rs:31` 모듈 doc 계수, CLI.md §6.17 표, CLI.md 산문 계수 두 곳이 한 덩어리로 움직인다 — 커밋을 쪼개면 `doctor_docs.rs:150`이 붉어진다. `:1051` 괄호의 재사용/신설 분해는 게이트가 없어 손으로 지켜야 한다.
- `qsh service`는 실기계 서비스 매니저 상태에 의존하는 첫 표면이다. M7 Step 6이 기록한 함정(실환경에서 발화하는데도 조건부 단언이 통과, qsh 미설치 CI에서는 완전히 공허)을 되풀이하지 않도록 환경 주입 스텁이 실제 조립 경로를 타게 한다.
- Windows leg. M7 Step 3·Step 5가 둘 다 `cfg(not(unix))` 블록 때문에 CI에서 붉어졌다. 각 스텝 게이트에 `cargo check --target x86_64-pc-windows-gnu`를 넣는다.
- 기존 fixture 불변이 `--as`·`assigned_name`의 `skip_serializing_if` 처리에 달려 있다. 빠뜨리면 `trust.invite.json`이 흔들리고 DoD 5가 즉시 깨진다. 주소 축은 위험이 아니다 — ADR-0014 결과(`:65`)가 열거한 8개 fixture는 `address`를 이미 `"<address>"`로 마스킹한다.
- 신규 fixture의 결정성. `cert_pem`(매 실행 새 키 재료의 파생물)과 `service.*`의 unit 경로(테스트별 `HOME` tempdir)가 `normalize` 마스크에 걸리지 않으면 golden이 회차마다 달라진다. Step 6이 `normalize`에 `cert_pem`을 더하고 Step 8이 필드를 `path`로 명명한다.
- M8 DoD 2 soak과 트리 분기. 통과 회차(run #6)는 `8c4f319`에서 돌았고 그 뒤 main에 얹힌 M9 커밋은 server·tunnel 경로를 바꾼다 — §7 태그 정책의 재지정 항목이 그 분리를 기록한다. 닫힌 DoD라 감시 항목에서는 빠지지만, 태그가 `8c4f319`를 가리키는지는 Step 11 절차 1이 확인한다.
- perf 게이트 flake 여지 — M8 Step 8 실측에서 dev 프로필 throughput 비율이 0.826까지 내려간 회차가 있었다(기준 0.80). M9는 데이터 경로를 건드리지 않으므로 0.80 아래가 나오면 러너 변동을 먼저 의심한다.

### 4.1 구현 중 확정할 값 (해당 step (a)에 근거와 함께 추기)

| # | 질문 | 초안 | 확정 시점 |
|---|---|---|---|
| 1 | 정규화 함수의 배치 | ADR-0014 결정 2(`:22`)가 요구한 `qsh-proto` 우선 검토를 실제로 수행하고 결론과 사유를 기록한다. `wire.rs`의 `format_host_port`/`parse_forward_spec` 옆이 후보이고(`qsh-cli` → `qsh-proto`는 arch 매트릭스상 허용) 그렇지 않으면 `qsh-core`. 기본 포트는 4433 하나이고 `serve.rs:21`의 `DEFAULT_BIND`가 쓰는 값과 같은 자리에서 나온다(새 "peer 기본 포트" 신설 없음 — 결정 1) | Step 1 |
| 2 | `code` 부재 판정의 소재 | `qsh-core`의 순수 resolver가 판정하고 CLI는 지시된 I/O만 수행, `TrustAcceptReq.code: String` 불변 | Step 2 |
| 3 | code 미제공 `INVALID_ARGUMENT`의 fixture | 기존 `error.INVALID_ARGUMENT.json` 재사용(신규 op가 아니라 DoD 5 대상 아님) | Step 2 |
| 4 | config 충돌 doctor code 문자열 | `config_serve_to_conflict`(status warn, doctor는 exit 0 유지) | Step 5 |
| 4b | CLI `--to`가 있을 때 두 config 키의 값 충돌 처분 | ADR-0012 결정 5는 `qsh serve`가 "이 순서로 읽되 둘 다 있고 값이 다르면 `CONFIG_ERROR`"까지만 적어 CLI가 우선하는 경우의 상호작용을 남겼다. 초안: CLI `--to`가 주어지면 config 두 키는 읽지 않으므로 충돌 판정도 하지 않는다(doctor는 여전히 finding을 낸다) | Step 5 |
| 5 | hide된 서브커맨드의 man 페이지 처분 | ADR-0012 결과(`:42`)가 구 man 3면 유지를 이미 정했다. 실측할 것은 `clap_mangen`이 `hide = true` 서브커맨드 페이지를 생성하는지 하나뿐이다. 생성하면 ADR과 자동으로 맞고, 생성하지 않으면 §8 열린 질문 6의 두 분기 중 하나 | Step 5 착수 전 |
| 6 | 신규 dotted op 이름 6종 | `identity.export`, `trust.add_ca`, `trust.rename`, `service.install`, `service.uninstall`, `service.status`. ADR-0013 결과(`:36`)가 `trust.add_ca`/`trust.addca` 중 확정을 CLI.md §2.4 개정 시점으로 넘겼으므로 그 선택의 사유를 남긴다 | Step 6·7·8 |
| 7 | `pair` 개명이 dotted 이름까지 가는지 | 가지 않는다 — CLI 표기만 바꾸고 dotted는 `trust.invite`/`trust.accept` 유지(`docs/CLI.md:94`의 계층 분리, ADR-0012 결정 9 `:37`). fixture·스키마·audit 무변경 | Step 7 |
| 8 | `serve --to` 모드의 unit 라벨 `<mode>` 토큰 | §8 열린 질문 2에 종속. Step 4는 토큰을 단일 상수로 격리만 하고 값은 Step 8에서 채운다 | Step 8 |
| 9 | 신규 doctor 6종의 status | 서비스 미등록 info, systemd linger 미설정 warn, macOS LaunchAgent 로그인 세션 한계 warn(ADR-0017 결과 `:46`의 예고), `bindv6only`·두 acl 진단은 이 스텝에서 정한다. 새 status 값은 만들지 않는다 | Step 4 |
| 10 | `qsh init` 유지 여부와 `qsh identity init` alias 신설 여부 | ADR-0013 결정 3(`:20`)이 "이 ADR이 결정하지 않는다"로 남겼다. 초안: `qsh init`을 그대로 두고 `qsh identity init`은 만들지 않는다(man 면은 `qsh-init.1` 유지 + `qsh-identity.1` 신설). 이 답이 man 파일 집합과 CLI.md §6.11 행을 바꾼다 | Step 6 |
| 11 | 대괄호 없는 순수 IPv6 리터럴(`::1` 류)의 포트 유무 판정 | ADR-0014 결정 2(`:24`)가 명시적으로 S1에 위임했다. `server_name_for`(`ops/mod.rs:932`)의 "가장 오른쪽 `:` 뒤가 전부 숫자면 포트" 규칙이 이런 리터럴을 오판할 수 있다. 저장소에 그런 사례는 없다 | Step 1 |
| 12 | peer 주소의 포트 범위를 M9가 새로 강제할지 | `docs/CLI.md:482`의 `1..=65535`·`0`/`65536` → `INVALID_ARGUMENT`는 `-L`/`-R` 터널 스펙 문법의 계약이고 peer 주소(`--address`, `pair accept <address>`, `--to`)를 규정하지 않는다. ADR-0014는 정규화만 정하고 범위 검증을 새로 계약하지 않는다. 초안: 강제하지 않고 정규화는 값을 손대지 않은 채 dial 시점 실패로 남긴다(강제하기로 하면 §6.11에 additive 문장이 필요하다) | Step 1 |
| 13 | 초대 출력 후보 주소의 출처 | 바인드된 소켓 / 로컬 인터페이스 열거 중 무엇을 쓰는지, `crates/qsh-core/src/doctor/probe.rs`를 재사용할 수 있는지 실측. human/stderr 전용이고 `trust.invite` envelope 필드는 0 추가 | Step 3 |
| 14 | 신규 fixture의 결정성 확보 방식 | `normalize`(`crates/qsh-testkit/src/fixtures.rs:73-170`)에 `cert_pem` 마스크를 더하고(기존 fixture 중 그 필드 보유 0건 실측) `service.*`의 unit 경로 필드는 기존 `path` 마스크에 걸리게 명명한다. `docs/design/testing.md:88`의 구조 동등성 대안은 DoD 5의 "신규 op마다 새 JSON fixture"와 충돌해 택하지 않는다 | Step 6·8 |

## 5. 완료 절차

1. §1 DoD 1~7 전건을 실제 테스트·캠페인 기록으로 확인(체크박스는 근거 green일 때만).
2. 구속 문서 태그 대조 — `docs/CLI.md` §2.3·§2.4·§2.5·§3.2·§6.11·§6.12·§6.13·§6.17·§10, `docs/PRD.md` §6·§11·§15, `docs/adr/0012`~`0017`의 M9 관련 전 문장 전수(Step 11 (a) 절차 1).
3. README 동기화 — 기능 목록·Known limitations·인터임 고지 + ADR 지정 델타 넷, Roadmap 표 M9 → Done. 서사·구조 전면 재작성 제외.
4. `docs/design/testing.md`에 M9 테스트 층 반영(Step 9 ⑤에서 이미 낸 문단의 최종 확인).
5. `docs/ROADMAP.md` "현재 위치"(`:5`)·M9 절 갱신 + 마감 노트.
6. §7 태그 정책을 ROADMAP(§2 또는 M10 절)에 한 줄로 이관 — PLAN.md가 교체돼도 정책이 남도록.
7. §0.2의 M7·M8 잔여 넷의 상태를 M10 계획에 승계(닫힌 것은 근거와 함께 종결로, 열린 것은 소유자와 함께 이월로).
8. 이 PLAN.md를 M10 계획으로 전면 교체(§3의 P1 재기록 항목, §6 이월 표의 미종결 행 이관 포함).

## 6. M8에서 이월된 항목

| # | 항목 | 소유 step |
|---|---|---|
| i | `hint_alias`(`crates/qsh-core/src/ops/host.rs:441`)가 벗긴 별칭의 조회 키 trim(remedy 문면에만 걸려 있다) — `hosts.toml`/`trust.toml` 이름 정규화와 같은 자리 | Step 1 (a)⑥. 출처: 교체 전 `PLAN.md:667`·`:675` |
| ii | doctor `acl_principal_unmatched`·`acl_ca_auth_path_missing`(threat model g4) | Step 4 (a)②. 출처: 교체 전 `PLAN.md:663`·`:675` |
| iii | `crates/qsh-core/src/resume.rs:320` `purge_expired` doc이 HEAD부터 틀림("Called by the same reaper pass…") | Step 9 (a)⑩. 출처: 교체 전 `PLAN.md:687` |
| iv | fuzz lock 117 패키지가 `cargo deny` 범위 밖(지금은 `fuzz/README`에 범위 밖 명시만) | M10 재기록 — 릴리스 CI 소관. 출처: 교체 전 `PLAN.md:687` |
| v | fuzz 크레이트가 edition 2021이라 `broker_ops` 하네스에 let-chain을 못 쓴다 | M9 범위 밖(정보성). 출처: 교체 전 `PLAN.md:687` |
| vi | CLOSE `0x1003`의 이중 이름(`RESOURCE_EXHAUSTED`/`REPLACED`) 처분 | M8 DoD 4의 §16 발효 소커밋(§0.2). 출처: 교체 전 `PLAN.md:675` |
| vii | `ControlLink`/`DataLink` enum → trait 전환(ADR-0005 P0 부채) | P1 재기록 — M9도 트리거하지 않음 |

## 7. 태그 정책

- 실측(2026-09-16): 저장소의 태그는 `v0.1.0-alpha.1`·`v0.1.0-alpha.2` 둘뿐이다 — `v0.1.0-alpha.3`은 아직 찍혀 있지 않다. `docs/ROADMAP.md:138`은 이 태그를 전제로 Homebrew tap 스켈레톤(`DaveDev42/homebrew-tap`, release.yml의 `homebrew-tap` job)을 미리 배선해 뒀다고 적고, formula sha256이 자리표시자라고 밝힌다. (2026-09-18 갱신: tap 쓰기 자격은 fine-grained PAT 가 아니라 tap 저장소의 write deploy key — 시크릿 `HOMEBREW_TAP_DEPLOY_KEY` — 로 바꿨고 시크릿은 등록돼 있다. 첫 릴리스의 formula 는 여전히 사람이 tap 에 밀어 넣는다.)
- 정책: `v0.1.0-alpha.3`은 HEAD가 아니라 soak-equivalent tree인 `525a2a5`(`build(deps): rustls 0.23.45 로 lockfile 갱신 — RUSTSEC-2026-0285 로 빨개진 cargo deny 게이트 복구`)에 찍는다. 근거는 M8 DoD 2의 판정 조건이다 — `docs/campaigns/m8-soak.md:27`이 "24h 내내 같은 바이너리(같은 sha256)로 재야" 한다고 회차 요건을 고정했고 `:41`이 트리 clean/dirty를 회차 기록에 남기라고 요구한다. 릴리스 태그가 HEAD를 가리키면 soak 판정 트리와 릴리스 트리가 갈라져 DoD 2의 근거가 무효가 된다.
- soak 회차가 실패하면 태그 대상은 통과 회차가 실제로 돈 트리로 재지정한다 — 그 트리의 커밋과 바이너리 sha256을 `docs/campaigns/m8-soak.md` §7 회차 표에서 인용한다.
- **재지정(2026-09-21).** run #5(dd66e0f, `525a2a5`와 동등 트리)는 FAIL이었고 run #6이 `8c4f319`(`test(m8): soak 회차 #6 계측을 넣는다`, 바이너리 sha256 `ee250603ff43d1097af5875caa7b31d2edc0d0f301f96cfd218526b745afc2ef`)에서 PASS했다(`m8-soak.md` §7 회차 표 6행). 위 규칙대로 `v0.1.0-alpha.3` 대상은 `8c4f319`다. 이 트리에는 `525a2a5` 뒤 36 커밋이 들어 있고 그중 M9 코드는 Step 1(d5eef05)·2(d71cb38)·3a(858acb5)·3b(51455dc)와 모듈 분할 리팩터다. `8c4f319` 뒤의 M9 커밋(Step 12의 `-D` 계열, f331cd0의 PTY 홈 폴백 등)은 server·tunnel 경로를 바꾸므로 다음 태그에 실린다. 태그 문면과 생성은 사람이 한다.
- M9 기간에 `8c4f319`를 가리키는 태그를 옮기지 않는다. M9 PLAN.md 작성 커밋 자체도 이 규칙 아래 있다. Step 0의 baseline 측정은 태그 대상과 별개로 M9 이전 표면인 `525a2a5`에서 한다 — 그 측정의 목적이 "현행 표면" 기록이라서다(§0.2 M7 DoD 1).
- 이 정책을 명문화한 file:line은 저장소에 없다(교체 전 `PLAN.md`에도 `docs/ROADMAP.md`에도 해당 문장이 없다 — 실측). 이 절이 그 명문화이고, §5.6이 Step 11 마감 커밋에서 ROADMAP으로 한 줄 이관한다.
- TAG-AUDIT 형식은 유지하고 대상만 M9로 다시 가리킨다 — Step 11의 (a) 선행 감사 / (b) 마감 커밋에 남길 것 / (c) 완료 판정 3필드, 절차 1은 "대조 문장 N건 — 검증 M, 후속 마일스톤 명시 유예 P, 열린 DoD 종속 Q, 대조 대상 아님 R, 충돌 S", 절차 2는 "대조 항목 N개 중 불일치 M", 산출물은 `$SP/step11/TAG-AUDIT.md`.

## 8. 열린 질문 (사용자 결정 필요)

2026-09-17: 사용자의 전면 자율 진행 지시에 따라 아래 다섯 건을 main 세션이 결정했다. 각 항목 끝의 **결정** 문장이 그것이다. 사용자가 뒤집으면 해당 스텝에서 되돌린다.

1. ADR-0014를 `승인됨`으로 확정할지 (Step 1 착수를 막는다). `docs/adr/README.md:3`이 `제안됨`을 "결정 절은 다 냈으나 사용자 확정 전 초안"으로 정의하고 ADR-0014는 색인(`:20`)과 파일 헤더(`0014-address-default-port.md:4`) 양쪽에서 그 상태다. 그 ADR의 결정 1~5·7·9가 Step 1·Step 5에서 코드로 착륙하므로, 확정 없이 열면 초안을 근거로 구현하고 구현을 근거로 상태를 올리는 순환이 된다. 확정 시 Step 10은 반영만 한다. **결정(2026-09-17):** `승인됨`으로 확정했다(§0.1 첫 항목). Step 10은 반영만 한다.
2. `serve --to` 모드의 unit 라벨. `docs/deploy/service.md:48`은 지금 `~/Library/LaunchAgents/io.qsh.reverse.plist`(label `io.qsh.reverse`)를 예시로 싣는다. 개명 뒤 이 모드의 `<mode>` 토큰을 `reverse`로 유지할지(기존 예시·이미 배포된 unit과 호환) `serve-to`로 바꿀지(새 명명과 일치). Step 4의 doctor 경로 상수와 Step 8의 install 대상이 같이 종속된다. Step 4는 토큰을 단일 상수로 격리해 막히지 않게 설계하므로 늦어도 Step 8 착수 전까지 필요하다. **결정(2026-09-17):** `reverse` 유지(§0.1 둘째 항목의 근거와 같다).
3. macOS plist의 정본. `qsh service install`이 생성하는 plist가 dave-environment의 `io.qsh.plist.j2`와 `KeepAlive` 의미론까지 일치해야 하는지. 그 템플릿은 이 저장소 밖이라 여기서 확인하지 않았다(UNVERIFIED). 일치를 요구하면 Step 8의 생성물이 그 템플릿을 정본으로 삼아야 하고, 아니면 `docs/deploy/service.md:17-42`가 정본이다. **결정(2026-09-17):** 정본은 이 저장소의 `docs/deploy/service.md`다. 저장소 밖 템플릿을 정본으로 삼지 않는다. 다만 Step 8의 생성물은 fleet에서 검증된 의미론을 그대로 갖춘다 — `RunAtLoad`, `KeepAlive` true, `ThrottleInterval` 15, `ProcessType` Background, `EnvironmentVariables`에 `HOME`과 `PATH`(`/opt/homebrew/bin:/usr/bin:/bin`)만 두고 `XDG_*`는 두지 않으며, 로그는 `~/Library/Logs/qsh/<mode>.out.log`/`.err.log`. dave-environment 쪽 템플릿은 이후 그 생성물을 따라간다.
4. 구 표기 호출 시 deprecation 경고를 낼지. ADR-0012 결정 4는 "v1 내내 숨김 서브커맨드 유지"만 정하고 경고 문면을 정하지 않는다(`0012-human-surface-naming.md:28`). 낸다면 그 한 줄이 Step 3의 축자 테스트 대상으로 하나 더 늘고, 상시 실행되는 `qsh reverse` 데몬의 stderr에 매 기동 한 줄이 추가된다. **결정(2026-09-17):** 경고를 내지 않는다. 구 표기는 조용한 숨김 alias다. 상주 데몬(`qsh reverse`)의 stderr에 매 기동 한 줄이 쌓이는 소음과 Step 3 축자 테스트 한 종 증가를 피한다. CLI.md에 "구 표기는 숨김이며 경고 없이 동작한다"를 한 줄 적는다.
5. Windows `UNSUPPORTED` 문면. `qsh listen`/`qsh reverse`는 이미 Windows에서 `UNSUPPORTED` + exit 255다(`docs/CLI.md:891`). `qsh service`의 세 모드도 같은 문면을 재사용할지, "서비스 설치는 P1"을 명시하는 별도 문면을 쓸지(유예 기능의 문면에 "P1 예정"을 넣는 관례 — `docs/ROADMAP.md:151-152`). **결정(2026-09-17):** 유예 기능 문면 관례(`docs/ROADMAP.md:151-152`)를 따라 "P1"을 담는다. 기존 `qsh listen`/`qsh reverse`의 Windows `UNSUPPORTED` 문면 상수에 이미 "P1"이 있으면 그 상수를 재사용하고, 없으면 `qsh service` 전용 상수 하나를 qsh-core에 둔다(문면 정본은 core, CLI는 출력만). 어느 쪽인지는 Step 8 착수 시 `docs/CLI.md` §6.13·§6.12의 실제 문면을 확인해 기록한다.
6. hide된 서브커맨드의 man 페이지 (조건부 — Step 5 착수 전 실측 결과에 달렸다). ADR-0012 결과(`:42`)는 `qsh-reverse.1`·`qsh-trust-invite.1`·`qsh-trust-accept.1`을 "숨김 서브커맨드 설명으로 유지"하기로 정했고 세 파일은 실측으로 `docs/man/`에 있다. 그런데 `cargo xtask man`은 체크인된 `*.1` 전부를 지우고 다시 렌더하며 파일 집합 동일성까지 byte-exact로 본다(`xtask/src/man.rs:93-124`). `clap_mangen`이 `hide = true` 서브커맨드 페이지를 생성하지 않으면 ADR의 유지 결정이 게이트와 충돌하므로 둘 중 하나를 골라야 한다 — (가) `man.rs`에 "생성기 밖 유지 파일" 예외 목록을 사유와 함께 신설, (나) Step 10에서 ADR-0012 결과 절의 man 문장을 개정. 생성한다면 이 질문은 닫힌다.

## 9. 리뷰 반영 기록

두 리뷰(`review-contract.md`, `review-tests.md`)의 발견 전건과 그 처분이다. 인용한 file:line은 전부 이 라운드에서 다시 열어 확인했다.

### review-contract.md

| 발견 | 처분 | 사유 / 반영 자리 |
|---|---|---|
| H1 읽기 정규화 지점이 ADR-0014 결정 4와 다르다 | 반영 | `resolve_host`를 빼고 `resolve_forward`(`ops/host.rs:231`)·`trust_list`(`ops/mod.rs:563`)로 교체, `source` 정규화-후-비교 항 추가, `hosts_toml.rs` 테스트와 `ops/host.rs` 파일 추가 — Step 1 (a)③④·(b)·(c) |
| H2 `제안됨` ADR을 구현 후 스스로 승격 | 반영 | §8 열린 질문 1 신설, §0.1에 Step 1 선행 조건, Step 10 ①을 "확정된 상태 반영"으로 축소 |
| H3 `accept_command` 값 변경 누락 | 반영 | Step 7 (a)③에 `ops/mod.rs:615` + 딸린 네 지점, (d)에 `PEER_UNTRUSTED.remedy`가 8종 문면과 충돌하지 않음 확인 항목 |
| H4 README 범위가 ADR 요구를 out으로 밀었다 | 반영 | §3 README 항을 "서사·구조 전면 재작성만 out"으로 고치고 델타를 Step 3·5·6·7에 배정, Step 9 ⑦은 잔여만, Step 11 절차 2 대조 항목에 델타 넷 |
| M1 config 우선순위가 테스트 가능하지 않다 | 반영 | Step 5 ④에 4단 순서 축자 이기, (c) 표를 `--to` 축으로 확장, §4.1 #4b 신설 |
| M2 alias 왕복이 인자 이름 차이를 못 덮는다 | 반영 | Step 5 ③에 `--offered-name`/`--name` 비대칭과 `[serve].name` 미신설, (c)에 수렴 단언과 `--help` 미노출 |
| M3 `controller_unreachable` 표면 개정 누락 | 반영 | Step 5 ⑥에 `doctor_connectivity_findings`(`ops/doctor.rs:379`)·CLI.md `:1010`·`:1055`·`ReverseConfig::controller` doc, (d)에 문면 상수 무변경 단언 |
| M4 8종 중 두 종의 소유 파일 오기 | 반영 | Step 3 (a)②의 소유 지점 표에 `serve.rs:83`·`reverse/listen.rs:262`, `-D`는 `ops/tunnel.rs:108`로 정정하고 clap 두 자리를 파싱 자리로 구분 |
| M5 페어링 고지 소유 지점 누락 + 문면 이중 확정 | 반영 | 소유 지점을 `server/mod.rs:2778`로, Step 3 (a)④에 2단계 문면 명시, Step 7 (a)⑤·(d)에 개정 의무. 두 리뷰가 제시한 "항목 전체를 Step 7로 이관" 대안은 택하지 않았다 — ADR-0017 결정 3(`:32`)이 `--as` 착지 전 구간의 문면을 따로 요구하므로 Step 3 판이 실제로 존재해야 한다 |
| M6 ADR-0017이 구현 스텝으로 넘긴 셋 누락 | 반영 | Step 4 (a)④ remedy 재시작 문면, ⑤ `minimal_policy_example` 역할별 확장(`acl/load.rs:373-374`), ⑥ `doctor.rs:31` 모듈 doc 계수; (b)에 `acl/load.rs`, (c)·(d)에 각 게이트 |
| M7 ADR-0013의 실패 모드·거부 규칙 다섯 누락 | 반영 | Step 6 (a)②③④에 상호배타·성공 무동작·유효기간 미검사·`--out` 충돌·identity 부재, ④를 "`add_ca` 호출 + `Ops` 가드 + doc 주석(`trust/mod.rs:272-277`) 갱신"으로 재작성, (c)④ negative 표를 3열로 |
| M8 §2.4 op 수가 27이다 | 반영 | Step 9 ①을 27 → 33으로, Step 6 (c) Layer 1에 `session.attach` 예외 승계. 실측 확인: `docs/CLI.md:65-91` 27개, `const COMMAND` 28건 중 트레이트 선언 1건 제외 |
| M9 §2.5 편집이 ADR-0012와 어긋난다 | 반영 | `trust.*` 와일드카드(`docs/CLI.md:116`)가 이미 덮으므로 토큰 추가는 `identity.export`와 `service.*` 3뿐, 표 아래 한 줄은 Step 5 ⑦ⓒ, `acl_registry.rs:254` 전개 갱신은 Step 6 Layer 3 |
| M10 fixture 마스킹 UNVERIFIED는 이미 닫힌다 | 반영 | Step 1 (c)를 ADR-0014 결과(`:65`)와 `crates/qsh-testkit/src/fixtures.rs:118` 인용으로 교체, §4 리스크에서 주소 축 제거 |
| M11 `assuming port 4433`의 세 번째 발화 지점 누락 | 반영 | Step 1 (a)⑤에 3지점, Step 5 (a)①에 발화, Step 3의 mutation을 발화 지점별로 재정의, §4.1 #1(qsh-proto 배치 검토)·#11(순수 IPv6 리터럴) 신설 |
| M12 포트 범위 근거가 다른 문법의 계약 | 반영 | Step 1 ⑦에서 `docs/CLI.md:482` 인용 제거(실측: 그 줄은 `-L`/`-R` 스펙 문법 절), §4.1 #12로 올려 초안은 "강제하지 않는다" |
| M13 hide된 man 처분은 ADR이 이미 정했다 | 반영 | §4.1 #5와 Step 5 ⑨를 "ADR-0012 결과 `:42`가 유지를 정했고 실측 대상은 `clap_mangen`의 hide 처리뿐"으로 재작성, 어긋나면 §8 열린 질문 6의 두 분기 |
| M14 man 면이 3이다 | 반영 | Step 6 (d)를 `qsh-identity.1`·`qsh-identity-export.1`·`qsh-trust-add-ca.1` 3면으로, §4.1 #10에 `qsh init`/`qsh identity init` 관계 |
| M15 `--code-stdin` 공백 제거 누락 | 반영 | Step 2 (a)①에 trim, ②에 도움말 두 줄, (c)에 개행·공백 입력 2건 |
| M16 ADR-0013의 CLI.md·README 상호 참조 누락 | 반영 | Step 6 (a)⑥에 §6.11 문장·§6.13 상호 참조·시나리오 B 왕복 네 줄·README 한 줄, (b)에 §6.13과 `README.md` |
| L1 alias 삭제의 컴파일 보호 서술이 절반만 맞다 | 반영 | §4 리스크를 `long_running_setup_mode`(`main.rs:910-917`, `_ => None`)와 `command_name`(`:1190-1225`, 완전 match)으로 갈라 씀 |
| L2 `trust_add`/`trust_accept` 줄번호가 marker impl | 반영 | `:506`/`:640`으로 교체하고 `:209-213`/`:223-227`은 "op 등록 자리"로 구분 |
| L3 CLI.md 산문 계수 줄번호와 미게이트 산술 | 반영 | Step 4 ①을 `docs/CLI.md:750`·`:1051`로 갱신하고 테스트 주석의 옛 번호와 구분, `:1051` 괄호 분해의 무게이트 사실을 (d)에 |
| L4 Step 4가 미해결 사용자 결정에 묶였다 | 반영 | 열린 질문 2를 §0.1로 올리고, Step 4 ⑧을 "토큰을 단일 상수로 격리"로 약화해 완료 판정에서 값을 뺌 |

### review-tests.md

F3=H1, F4=M4, F5=M13, F6=H4, F7=H3, F11=M14, F13=M9, F14=M15, F15=M6, F16=M5, F17=M11, F18=M1+M2, F19=M3, F20=M7+M16, F21=M7, F22=M8, F24=M10은 위 표의 같은 처분이다. 나머지:

| 발견 | 처분 | 사유 / 반영 자리 |
|---|---|---|
| F1 `auth_path` 문면을 Step 3이 소유할 수 없다 | 반영 | `Diagnostic`이 `id: DiagnosticId`를 필수로 갖는다는 실측(`doctor.rs:116-122`)대로 그 1종을 Step 4로 이관, §1 DoD 3 근거를 "Step 3(7종) + Step 4(1종)"으로, Step 3 제목도 7종으로 |
| F2 "초대 출력 후보 주소" 항목이 배분에서 빠졌다 | 반영 | Step 3 (a)②의 소유 지점 표에 8번째 행, ③에 출처·envelope 필드 0·`jsonl_purity` 회귀, §4.1 #13 신설 |
| F8 `identity.export`·`service.*` fixture 비결정성 | 반영 | Step 6 (c)②에 `normalize` `cert_pem` 마스크(기존 fixture 보유 0건 실측), Step 8 (a)⑧에 `path` 명명, §4.1 #14. 구조 동등성 대안은 기각 — DoD 5(`docs/ROADMAP.md:125`)가 "신규 op마다 새 JSON fixture"를 요구하므로 계획이 ROADMAP 문면을 바꿀 수 없다 |
| F9 SC1 baseline이 스텝 순서에 자리가 없다 | 반영 | Step 0(사람, 크기 내역 밖) 신설 — `525a2a5` 트리 측정 + 바이너리 sha256 기록, Step 1 (a) 선행 줄에 "이후에는 태그 트리 재빌드로만 가능" |
| F10 6면 중 JSON 렌더러 면이 관측 불가 | 반영 | Step 6 (c) Layer 2에서 JSON 면을 (fixture `command` 일치) 또는 (dispatch의 `Envelope::success(<Op>::COMMAND, …)` 소스 스캔)으로 재정의. 실측: `render/json.rs`에 op별 분기 없음(`:22`·`:34`·`:52`·`:65` 넷뿐) |
| F12 `qsh identity` 그룹 신설 결정이 §4.1에 없다 | 반영 | §4.1 #10 신설, Step 6 (b) 라벨을 "`Command::Init` 부근 + 신설 `Command::Identity`"로(실측: `cli.rs:221-229`는 `Command::Init`) |
| F23 이월 표 두 행이 소유 스텝 본문에 없다 | 반영 | Step 1 (a)⑥에 `hint_alias` 조회 키 trim(`ops/host.rs:441`), Step 9 (a)⑩에 `resume.rs:320` 주석 정정 |
| F25 §1 DoD 1 줄에 소유자 표기 없음 | 반영 | DoD 1 줄 끝에 "소유: 사람 … 에이전트 몫은 캠페인 문서 사전 정의뿐" |
| F26 태그 정책에 soak 실패 분기 없음 | 반영 | §7에 "회차가 실패하면 통과 회차가 돈 트리로 재지정" 한 줄 |
